# Directory Structure Report

**Project:** TeamTrau_Downloader_Anime
**Generated:** 2026-08-13 03:00:04 UTC
**Filters:** py, bat, ps1, toml, md
**Ignored:** docs, target, .git, node_modules, artifacts, build, __pycache__, .venv, venv, .pytest_cache, .env

## Change Summary

- Added: `run_anikoto.bat`
- Added: `run_animecube.bat`
- Added: `scratch\debug_animecube_sources.py`
- Added: `scratch\debug_dailymotion_embed.py`
- Added: `scratch\inspect_animecube.py`
- Added: `scratch\test_animecube_all.py`
- Added: `scratch\test_dailymotion_extractor.py`
- Added: `scratch\test_demon_hunter.py`
- Added: `scratch\test_dm_keys.py`
- Added: `scratch\test_dm_qualities.py`
- Added: `scratch\test_dmxleo.py`
- Added: `scratch\test_ling_cage.py`
- Added: `src\downloader\animecube.py`
- Added: `src\downloader\browser_sniffer.py`
- Added: `src\downloader\hls_downloader.py`
- Added: `src\downloader\subtitle_scraper.py`
- Added: `tests\test_animecube.py`
- Added: `tests\test_browser_sniffer.py`
- Modified: `run.bat`
- Modified: `src\downloader\__init__.py`
- Modified: `src\downloader\cli.py`
- Modified: `src\downloader\core.py`
- Modified: `src\downloader\utils.py`

## Added Files

### File: `run_anikoto.bat`

_Status: Added_

```text
   1 | @echo off
   2 | setlocal enabledelayedexpansion
   3 | title AniKoto Downloader CLI - anikototv.to
   4 | cls
   5 | 
   6 | cd /d "%~dp0"
   7 | 
   8 | if not exist ".venv" (
   9 |     echo [ERROR] Virtual environment .venv not found.
  10 |     echo Please run setup_env_py.bat first to set up the environment.
  11 |     pause
  12 |     exit /b 1
  13 | )
  14 | 
  15 | if not "%~1"=="" (
  16 |     .venv\Scripts\python.exe run_app.py -u "%~1" %2 %3 %4 %5 %6 %7 %8 %9
  17 |     echo.
  18 |     pause
  19 |     exit /b !ERRORLEVEL!
  20 | )
  21 | 
  22 | echo ============================================================
  23 | echo      ANIKOTO.TO - BATCH ANIME DOWNLOADER MODULE
  24 | echo ============================================================
  25 | echo.
  26 | echo Target Site : https://anikototv.to
  27 | echo Example URL : https://anikototv.to/watch/demon-slayer
  28 | echo.
  29 | echo Please answer the following prompts (Press Enter for defaults).
  30 | echo.
  31 | 
  32 | set /p ANIME_URL="[1/6] Enter AniKoto URL: "
  33 | if not defined ANIME_URL (
  34 |     echo [ERROR] Anime URL cannot be empty!
  35 |     pause
  36 |     exit /b 1
  37 | )
  38 | 
  39 | for /f "delims=" %%I in ("!ANIME_URL!") do set ANIME_URL=%%~I
  40 | 
  41 | echo.
  42 | set /p EPISODES="[2/6] Episode Range (e.g. all, 1-5) [all]: "
  43 | if not defined EPISODES set EPISODES=all
  44 | 
  45 | set /p SUB_LANG="[3/6] Subtitle Language (e.g. en, vi) [en]: "
  46 | if not defined SUB_LANG set SUB_LANG=en
  47 | 
  48 | set /p OUTPUT_DIR="[4/6] Output Directory [.\downloads]: "
  49 | if not defined OUTPUT_DIR set OUTPUT_DIR=.\downloads
  50 | 
  51 | echo.
  52 | echo [5/6] Select Download Mode:
  53 | echo   [1] Subtitles Only
  54 | echo   [2] Videos Only
  55 | echo   [3] Both Subtitles + Videos (Default)
  56 | echo.
  57 | set /p MODE_CHOICE="Enter Choice (1/2/3) [3]: "
  58 | if not defined MODE_CHOICE set MODE_CHOICE=3
  59 | 
  60 | set EXTRA_ARGS=
  61 | if "!MODE_CHOICE!"=="1" set EXTRA_ARGS=--sub-only
  62 | if "!MODE_CHOICE!"=="2" set EXTRA_ARGS=--video-only
  63 | 
  64 | echo.
  65 | set /p INTERACTIVE_CHOICE="[6/8] Enable Interactive Server Selection (y/n) [n]: "
  66 | if /i "!INTERACTIVE_CHOICE:~0,1!"=="y" (
  67 |     set EXTRA_ARGS=!EXTRA_ARGS! --interactive
  68 | )
  69 | 
  70 | echo.
  71 | set /p PROXY_URL="[7/8] Optional HTTP/SOCKS5 Proxy (e.g. http://127.0.0.1:8080 or Enter for none): "
  72 | if defined PROXY_URL (
  73 |     for /f "delims=" %%I in ("!PROXY_URL!") do set PROXY_URL=%%~I
  74 |     set EXTRA_ARGS=!EXTRA_ARGS! --proxy "!PROXY_URL!"
  75 | )
  76 | 
  77 | echo.
  78 | set /p SNIFFER_CHOICE="[8/8] Enable Playwright Browser Network Sniffer (y/n) [n]: "
  79 | if /i "!SNIFFER_CHOICE:~0,1!"=="y" (
  80 |     set EXTRA_ARGS=!EXTRA_ARGS! --use-browser-sniffer
  81 | )
  82 | 
  83 | echo.
  84 | echo ------------------------------------------------------------
  85 | echo  Starting AniKoto Download...
  86 | echo  URL      : !ANIME_URL!
  87 | echo  Episodes : !EPISODES!
  88 | echo  Language : !SUB_LANG!
  89 | echo  Output   : !OUTPUT_DIR!
  90 | echo  Extra    : !EXTRA_ARGS!
  91 | echo ------------------------------------------------------------
  92 | echo.
  93 | 
  94 | .venv\Scripts\python.exe run_app.py -u "!ANIME_URL!" -e "!EPISODES!" -l "!SUB_LANG!" -o "!OUTPUT_DIR!" !EXTRA_ARGS!
  95 | 
  96 | echo.
  97 | echo ============================================================
  98 | echo  Download session finished! Press any key to return/exit.
  99 | echo ============================================================
 100 | pause
```

### File: `run_animecube.bat`

_Status: Added_

```text
   1 | @echo off
   2 | setlocal enabledelayedexpansion
   3 | title AnimeCube Downloader CLI - animecube.live
   4 | cls
   5 | 
   6 | :: Change directory to script folder
   7 | cd /d "%~dp0"
   8 | 
   9 | :: Check virtual environment
  10 | if not exist ".venv" (
  11 |     echo [ERROR] Virtual environment .venv not found.
  12 |     echo Please run setup_env_py.bat first to set up the environment.
  13 |     pause
  14 |     exit /b 1
  15 | )
  16 | 
  17 | :: If URL argument is provided via command line, execute directly
  18 | if not "%~1"=="" (
  19 |     .venv\Scripts\python.exe run_app.py -u "%~1" %2 %3 %4 %5 %6 %7 %8 %9
  20 |     echo.
  21 |     pause
  22 |     exit /b !ERRORLEVEL!
  23 | )
  24 | 
  25 | :: ============================================================
  26 | ::   ANIMECUBE MODULE INTERACTIVE MENU
  27 | :: ============================================================
  28 | echo ============================================================
  29 | echo      ANIMECUBE.LIVE - BATCH ANIME DOWNLOADER MODULE
  30 | echo ============================================================
  31 | echo.
  32 | echo Target Site : https://animecube.live
  33 | echo Example URL : https://animecube.live/anime/ling-cage?season=tab-1^&episode=ling-cage-tab-1-ep-SP
  34 | echo.
  35 | echo Please answer the following prompts (Press Enter for defaults).
  36 | echo.
  37 | 
  38 | set /p ANIME_URL="[1/6] Enter AnimeCube URL: "
  39 | if not defined ANIME_URL (
  40 |     echo [ERROR] Anime URL cannot be empty!
  41 |     pause
  42 |     exit /b 1
  43 | )
  44 | 
  45 | :: Strip surrounding quotes if user entered them
  46 | for /f "delims=" %%I in ("!ANIME_URL!") do set ANIME_URL=%%~I
  47 | 
  48 | echo.
  49 | set /p EPISODES="[2/6] Episode Range (e.g. all, 1, 1-5, SP) [all]: "
  50 | if not defined EPISODES set EPISODES=all
  51 | 
  52 | set /p SUB_LANG="[3/6] Subtitle Language (e.g. en, vi) [en]: "
  53 | if not defined SUB_LANG set SUB_LANG=en
  54 | 
  55 | set /p OUTPUT_DIR="[4/6] Output Directory [.\downloads]: "
  56 | if not defined OUTPUT_DIR set OUTPUT_DIR=.\downloads
  57 | 
  58 | echo.
  59 | echo [5/6] Select Download Mode:
  60 | echo   [1] Subtitles Only (Fast)
  61 | echo   [2] Videos Only
  62 | echo   [3] Both Subtitles + Videos (Default)
  63 | echo.
  64 | set /p MODE_CHOICE="Enter Choice (1/2/3) [3]: "
  65 | if not defined MODE_CHOICE set MODE_CHOICE=3
  66 | 
  67 | set EXTRA_ARGS=
  68 | if "!MODE_CHOICE!"=="1" set EXTRA_ARGS=--sub-only
  69 | if "!MODE_CHOICE!"=="2" set EXTRA_ARGS=--video-only
  70 | 
  71 | echo.
  72 | set /p INTERACTIVE_CHOICE="[6/7] Enable Interactive Server Selection (y/n) [n]: "
  73 | if /i "!INTERACTIVE_CHOICE:~0,1!"=="y" (
  74 |     set EXTRA_ARGS=!EXTRA_ARGS! --interactive
  75 | )
  76 | 
  77 | echo.
  78 | set /p PROXY_URL="[7/8] Optional HTTP/SOCKS5 Proxy (e.g. http://127.0.0.1:8080 or Enter for none): "
  79 | if defined PROXY_URL (
  80 |     for /f "delims=" %%I in ("!PROXY_URL!") do set PROXY_URL=%%~I
  81 |     set EXTRA_ARGS=!EXTRA_ARGS! --proxy "!PROXY_URL!"
  82 | )
  83 | 
  84 | echo.
  85 | set /p SNIFFER_CHOICE="[8/8] Enable Playwright Browser Network Sniffer (y/n) [n]: "
  86 | if /i "!SNIFFER_CHOICE:~0,1!"=="y" (
  87 |     set EXTRA_ARGS=!EXTRA_ARGS! --use-browser-sniffer
  88 | )
  89 | 
  90 | echo.
  91 | echo ------------------------------------------------------------
  92 | echo  Starting AnimeCube Download...
  93 | echo  URL      : !ANIME_URL!
  94 | echo  Episodes : !EPISODES!
  95 | echo  Language : !SUB_LANG!
  96 | echo  Output   : !OUTPUT_DIR!
  97 | echo  Extra    : !EXTRA_ARGS!
  98 | echo ------------------------------------------------------------
  99 | echo.
 100 | 
 101 | .venv\Scripts\python.exe run_app.py -u "!ANIME_URL!" -e "!EPISODES!" -l "!SUB_LANG!" -o "!OUTPUT_DIR!" !EXTRA_ARGS!
 102 | 
 103 | echo.
 104 | echo ============================================================
 105 | echo  Download session finished! Press any key to return/exit.
 106 | echo ============================================================
 107 | pause
```

### File: `scratch\debug_animecube_sources.py`

_Status: Added_

```text
   1 | import sys, os, json, base64, hashlib, re
   2 | from curl_cffi import requests
   3 | from cryptography.hazmat.primitives.ciphers.aead import AESGCM
   4 | 
   5 | sys.path.insert(0, os.path.abspath("src"))
   6 | 
   7 | headers = {
   8 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   9 |     "Referer": "https://animecube.live/"
  10 | }
  11 | 
  12 | url = "https://animecube.live/anime/ling-cage"
  13 | r = requests.get(url, headers=headers, impersonate="chrome120")
  14 | text = r.text
  15 | 
  16 | pushes = re.findall(r'self\.__next_f\.push\((.*?)\)</script>', text, re.DOTALL)
  17 | full_str = ""
  18 | for p in pushes:
  19 |     try:
  20 |         data = json.loads(p)
  21 |         if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
  22 |             full_str += data[1]
  23 |     except Exception:
  24 |         pass
  25 | 
  26 | pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
  27 | if pt_match:
  28 |     primary_tabs = json.loads(pt_match.group(1))
  29 |     print("Primary tabs found:", len(primary_tabs))
  30 |     for ptab in primary_tabs:
  31 |         p_id = ptab.get("id")
  32 |         p_name = ptab.get("customName") or ptab.get("title")
  33 |         for season in ptab.get("seasons", []):
  34 |             s_id = season.get("id")
  35 |             s_name = season.get("title")
  36 |             eps = season.get("episodes", [])
  37 |             print(f"\nTab '{p_name}' ({p_id}) -> Season '{s_name}' ({s_id}) -> {len(eps)} episodes")
  38 | 
  39 |             reg_url = "https://animecube.live/api/anime-sources-versions"
  40 |             reg_json = requests.get(reg_url, headers=headers, impersonate="chrome120").json()
  41 |             version_hash = reg_json.get("bySeason", {}).get("ling-cage", {}).get(p_id, {}).get(s_id)
  42 |             print("  Version hash:", version_hash)
  43 | 
  44 |             if version_hash and eps:
  45 |                 for ep in eps[:3]:
  46 |                     ep_id = ep.get("id")
  47 |                     x_obf = os.urandom(16).hex()
  48 |                     sources_url = (
  49 |                         f"https://animecube.live/api/anime/ling-cage/episode/{ep_id}/sources"
  50 |                         f"?v={version_hash}&primaryTabId={p_id}&seasonId={s_id}"
  51 |                     )
  52 |                     h = {"Accept": "application/json", "X-Obf": x_obf, "Referer": "https://animecube.live/anime/ling-cage"}
  53 |                     res_json = requests.get(sources_url, headers=h, impersonate="chrome120").json()
  54 |                     enc_payload = res_json.get("d")
  55 |                     if enc_payload:
  56 |                         raw = base64.b64decode(enc_payload)
  57 |                         iv = raw[:12]
  58 |                         ciphertext = raw[12:]
  59 |                         salt = f"{x_obf}|{version_hash}".encode()
  60 |                         key = hashlib.sha256(salt).digest()
  61 |                         aesgcm = AESGCM(key)
  62 |                         decrypted_bytes = aesgcm.decrypt(iv, ciphertext, None)
  63 |                         sources_data = json.loads(decrypted_bytes.decode("utf-8"))
  64 |                         print(f"\n  Ep {ep_id} -> sources count: {len(sources_data.get('sources', []))}")
  65 |                         for s in sources_data.get("sources", []):
  66 |                             print(f"    Source: platform={s.get('platform')}, videoId={s.get('videoId')}, quality={s.get('quality')}, url={s.get('url')}")
  67 |                         print(f"    Subtitles: {sources_data.get('subtitles')}")
```

### File: `scratch\debug_dailymotion_embed.py`

_Status: Added_

```text
   1 | import sys, json
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
   6 |     "Referer": "https://animecube.live/"
   7 | }
   8 | 
   9 | # Allowed fields in Dailymotion API: id, title, embed_url, private, duration, mode
  10 | api_url = "https://api.dailymotion.com/video/x9ps6dc?fields=id,title,embed_url,private,duration,mode"
  11 | r1 = requests.get(api_url, headers=headers, impersonate="chrome120")
  12 | print("API Dailymotion Status:", r1.status_code)
  13 | print("API Response:", r1.text)
```

### File: `scratch\inspect_animecube.py`

_Status: Added_

```text
   1 | import sys, json, re
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
   6 |     "Referer": "https://animecube.live/"
   7 | }
   8 | 
   9 | r = requests.get("https://animecube.live/anime/the-demon-hunter", headers=headers, impersonate="chrome120")
  10 | text = r.text
  11 | 
  12 | pushes = re.findall(r'self\.__next_f\.push\((.*?)\)</script>', text, re.DOTALL)
  13 | full_str = ""
  14 | for p in pushes:
  15 |     try:
  16 |         data = json.loads(p)
  17 |         if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
  18 |             full_str += data[1]
  19 |     except Exception:
  20 |         pass
  21 | 
  22 | # Extract primaryTabs array structure
  23 | # Look for "primaryTabs":[{...}] or similar
  24 | pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
  25 | if pt_match:
  26 |     primary_tabs = json.loads(pt_match.group(1))
  27 |     print("Parsed primaryTabs count:", len(primary_tabs))
  28 |     total_eps = 0
  29 |     for ptab in primary_tabs:
  30 |         p_id = ptab.get("id", "primary-1")
  31 |         p_name = ptab.get("customName") or ptab.get("title") or p_id
  32 |         for season in ptab.get("seasons", []):
  33 |             s_id = season.get("id", "tab-1")
  34 |             s_name = season.get("title") or s_id
  35 |             eps = season.get("episodes", [])
  36 |             print(f"PrimaryTab '{p_name}' ({p_id}) -> Season '{s_name}' ({s_id}): {len(eps)} episodes")
  37 |             total_eps += len(eps)
  38 |     print("Total parsed episodes:", total_eps)
  39 | else:
  40 |     print("primaryTabs match not found")
```

### File: `scratch\test_animecube_all.py`

_Status: Added_

```text
   1 | import sys, os, json
   2 | sys.path.insert(0, os.path.abspath("src"))
   3 | 
   4 | from downloader.animecube import AnimeCubeExtractor
   5 | from downloader.utils import HttpClient
   6 | 
   7 | client = HttpClient()
   8 | extractor = AnimeCubeExtractor(client)
   9 | 
  10 | anime_list = [
  11 |     "https://animecube.live/anime/battle-through-the-heavens",
  12 |     "https://animecube.live/anime/swallowed-star",
  13 |     "https://animecube.live/anime/renegade-immortal",
  14 |     "https://animecube.live/anime/perfect-world",
  15 |     "https://animecube.live/anime/martial-universe",
  16 |     "https://animecube.live/anime/shrouding-the-heavens",
  17 |     "https://animecube.live/anime/stellar-transformations"
  18 | ]
  19 | 
  20 | print("--- Testing AnimeCubeExtractor across popular anime titles ---")
  21 | for url in anime_list:
  22 |     slug = url.split("/")[-1]
  23 |     try:
  24 |         details = extractor.get_anime_details(url)
  25 |         eps = details.get("episodes", [])
  26 |         print(f"\nAnime: {details.get('title')} ({slug}) -> Total eps: {len(eps)}")
  27 |         if eps:
  28 |             ep1 = eps[0]
  29 |             ep_data = extractor.get_episode_data(ep1, lang="en")
  30 |             v_url = ep_data.get("video_url")
  31 |             print(f"  Ep 1 ({ep1.get('slug')}) -> Video URL: {v_url[:90] if v_url else 'None (Unavailable/Private)'}")
  32 |     except Exception as e:
  33 |         print(f"  Error on {slug}: {e}")
```

### File: `scratch\test_dailymotion_extractor.py`

_Status: Added_

```text
   1 | import sys, json, re
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   6 |     "Referer": "https://animecube.live/",
   7 |     "Origin": "https://animecube.live"
   8 | }
   9 | 
  10 | vids = [
  11 |     ("x9q0yw0", "k7IN2pETI8HhhVDMOsw"),
  12 |     ("x9nm6ji", "k3jihnvMLd02FHDvP5A"),
  13 |     ("x9ps1gs", "k51PdThRbOubKUDL45K")
  14 | ]
  15 | 
  16 | for video_id, private_id in vids:
  17 |     auth_param = f"?auth={private_id}" if private_id else ""
  18 |     meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}{auth_param}&embedder=https%3A%2F%2Fanimecube.live%2F"
  19 |     r = requests.get(meta_url, headers=headers, impersonate="chrome120")
  20 |     print(f"\n--- Video {video_id} --- Status: {r.status_code}")
  21 |     data = r.json()
  22 |     if "error" in data:
  23 |         print("  Error code:", data["error"].get("code"))
  24 |     else:
  25 |         print("  Title:", data.get("title"))
  26 |         ad_url = data.get("advertising", {}).get("ad_url")
  27 |         print("  ad_url:", ad_url[:100] if ad_url else "None")
```

### File: `scratch\test_demon_hunter.py`

_Status: Added_

```text
   1 | import sys, os, json, base64, hashlib, re
   2 | from curl_cffi import requests
   3 | from cryptography.hazmat.primitives.ciphers.aead import AESGCM
   4 | 
   5 | headers = {
   6 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   7 |     "Referer": "https://animecube.live/"
   8 | }
   9 | 
  10 | url = "https://animecube.live/anime/the-demon-hunter"
  11 | r = requests.get(url, headers=headers, impersonate="chrome120")
  12 | text = r.text
  13 | 
  14 | pushes = re.findall(r'self\.__next_f\.push\((.*?)\)</script>', text, re.DOTALL)
  15 | full_str = ""
  16 | for p in pushes:
  17 |     try:
  18 |         data = json.loads(p)
  19 |         if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
  20 |             full_str += data[1]
  21 |     except Exception:
  22 |         pass
  23 | 
  24 | pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
  25 | if pt_match:
  26 |     primary_tabs = json.loads(pt_match.group(1))
  27 |     print(f"Primary tabs count: {len(primary_tabs)}", flush=True)
  28 | 
  29 |     reg_url = "https://animecube.live/api/anime-sources-versions"
  30 |     reg_json = requests.get(reg_url, headers=headers, impersonate="chrome120").json()
  31 | 
  32 |     for ptab in primary_tabs:
  33 |         p_id = ptab.get("id")
  34 |         p_name = ptab.get("customName") or ptab.get("title")
  35 |         p_type = ptab.get("type")
  36 |         for season in ptab.get("seasons", []):
  37 |             s_id = season.get("id")
  38 |             s_name = season.get("title")
  39 |             eps = season.get("episodes", [])
  40 |             print(f"\nTab: '{p_name}' ({p_id}, type={p_type}) -> Season: '{s_name}' ({s_id}) -> {len(eps)} eps", flush=True)
  41 | 
  42 |             version_hash = reg_json.get("bySeason", {}).get("the-demon-hunter", {}).get(p_id, {}).get(s_id)
  43 |             print("  Version hash:", version_hash, flush=True)
  44 | 
  45 |             if version_hash and eps:
  46 |                 # Check first 5 episodes
  47 |                 for ep in eps[:5]:
  48 |                     ep_num = str(ep.get("numberDisplay") or ep.get("number") or "")
  49 |                     ep_id = ep.get("id")
  50 |                     x_obf = os.urandom(16).hex()
  51 |                     sources_url = (
  52 |                         f"https://animecube.live/api/anime/the-demon-hunter/episode/{ep_id}/sources"
  53 |                         f"?v={version_hash}&primaryTabId={p_id}&seasonId={s_id}"
  54 |                     )
  55 |                     h = {"Accept": "application/json", "X-Obf": x_obf, "Referer": "https://animecube.live/anime/the-demon-hunter"}
  56 |                     res_json = requests.get(sources_url, headers=h, impersonate="chrome120").json()
  57 |                     enc_payload = res_json.get("d")
  58 |                     if enc_payload:
  59 |                         raw = base64.b64decode(enc_payload)
  60 |                         iv = raw[:12]
  61 |                         ciphertext = raw[12:]
  62 |                         salt = f"{x_obf}|{version_hash}".encode()
  63 |                         key = hashlib.sha256(salt).digest()
  64 |                         aesgcm = AESGCM(key)
  65 |                         decrypted_bytes = aesgcm.decrypt(iv, ciphertext, None)
  66 |                         sources_data = json.loads(decrypted_bytes.decode("utf-8"))
  67 | 
  68 |                         for s in sources_data.get("sources", []):
  69 |                             vid_id = s.get("videoId")
  70 |                             priv_id = s.get("privateId")
  71 |                             plat = s.get("platform")
  72 |                             if plat == "dailymotion" and vid_id:
  73 |                                 auth_str = f"auth={priv_id}&" if priv_id else ""
  74 |                                 m_url = f"https://www.dailymotion.com/player/metadata/video/{vid_id}?{auth_str}embedder=https%3A%2F%2Fanimecube.live%2F"
  75 |                                 meta_res = requests.get(m_url, headers=headers, impersonate="chrome120").json()
  76 |                                 err = meta_res.get("error", {}).get("code") if meta_res.get("error") else "NONE"
  77 |                                 quals = meta_res.get("qualities", {})
  78 |                                 print(f"  Ep {ep_num} ({ep_id}) -> Dailymotion {vid_id} -> Error: {err}, Streams: {list(quals.keys())}", flush=True)
```

### File: `scratch\test_dm_keys.py`

_Status: Added_

```text
   1 | import sys, json
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   6 |     "Referer": "https://animecube.live/"
   7 | }
   8 | 
   9 | video_id = "x9or71s"
  10 | auth = "k6JNTDpO47cP6mDDQTm"
  11 | meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}?auth={auth}&embedder=https%3A%2F%2Fanimecube.live%2F"
  12 | 
  13 | r = requests.get(meta_url, headers=headers, impersonate="chrome120")
  14 | data = r.json()
  15 | 
  16 | print("Root keys in metadata JSON:", list(data.keys()))
  17 | for k, v in data.items():
  18 |     if k != "advertising":
  19 |         v_str = str(v).encode("ascii", "replace").decode("ascii")
  20 |         print(f"  {k}: {type(v).__name__} -> {v_str[:120]}")
```

### File: `scratch\test_dm_qualities.py`

_Status: Added_

```text
   1 | import sys, json
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   6 |     "Referer": "https://animecube.live/"
   7 | }
   8 | 
   9 | # Test a known public Dailymotion video
  10 | video_id = "x8x5v4c" # Public video
  11 | meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}?embedder=https%3A%2F%2Fanimecube.live%2F"
  12 | 
  13 | r = requests.get(meta_url, headers=headers, impersonate="chrome120")
  14 | data = r.json()
  15 | 
  16 | print("Status:", r.status_code)
  17 | print("Title:", data.get("title"))
  18 | print("Has error:", "error" in data)
  19 | qualities = data.get("qualities", {})
  20 | print("Qualities keys:", list(qualities.keys()))
  21 | 
  22 | for res_name, res_list in qualities.items():
  23 |     print(f"\nResolution: {res_name}")
  24 |     for item in res_list:
  25 |         print("  Type:", item.get("type"), "-> URL:", item.get("url")[:100])
```

### File: `scratch\test_dmxleo.py`

_Status: Added_

```text
   1 | import sys
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   6 |     "Referer": "https://www.dailymotion.com/",
   7 |     "Origin": "https://www.dailymotion.com"
   8 | }
   9 | 
  10 | # 1. Fetch Dailymotion metadata for Perfect World Ep 228
  11 | video_id = "x9or71s"
  12 | auth = "k6JNTDpO47cP6mDDQTm"
  13 | meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}?auth={auth}&embedder=https%3A%2F%2Fanimecube.live%2F"
  14 | 
  15 | r1 = requests.get(meta_url, headers=headers, impersonate="chrome120")
  16 | print("Meta status:", r1.status_code)
  17 | data = r1.json()
  18 | ad_url = data.get("advertising", {}).get("ad_url")
  19 | print("ad_url:", ad_url)
  20 | 
  21 | if ad_url:
  22 |     # 2. Fetch the m3u8 playlist using curl_cffi
  23 |     r2 = requests.get(ad_url, headers=headers, impersonate="chrome120")
  24 |     print("\nm3u8 Status:", r2.status_code)
  25 |     print("m3u8 Content-Type:", r2.headers.get("content-type"))
  26 |     print("m3u8 text snippet:\n", r2.text[:500])
```

### File: `scratch\test_ling_cage.py`

_Status: Added_

```text
   1 | import sys, json
   2 | from curl_cffi import requests
   3 | 
   4 | headers = {
   5 |     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
   6 |     "Referer": "https://animecube.live/",
   7 |     "Origin": "https://animecube.live",
   8 |     "Sec-Fetch-Dest": "empty",
   9 |     "Sec-Fetch-Mode": "cors",
  10 |     "Sec-Fetch-Site": "cross-site",
  11 | }
  12 | 
  13 | # Test metadata request with cross-site headers
  14 | url = "https://www.dailymotion.com/player/metadata/video/x9q0yw0?auth=k7IN2pETI8HhhVDMOsw"
  15 | r = requests.get(url, headers=headers, impersonate="chrome120")
  16 | print("Status:", r.status_code)
  17 | data = r.json()
  18 | print("Keys:", list(data.keys()))
  19 | if "error" in data:
  20 |     print("Error:", data["error"])
  21 | else:
  22 |     print("Qualities:", list(data.get("qualities", {}).keys()))
  23 |     print("Advertising ad_url:", data.get("advertising", {}).get("ad_url"))
```

### File: `src\downloader\animecube.py`

_Status: Added_

```text
   1 | import base64
   2 | import hashlib
   3 | import json
   4 | import os
   5 | import re
   6 | from urllib.parse import urlparse
   7 | 
   8 | from cryptography.hazmat.primitives.ciphers.aead import AESGCM
   9 | 
  10 | from downloader.extractor import BaseExtractor, register_extractor
  11 | from downloader.utils import console
  12 | 
  13 | 
  14 | @register_extractor
  15 | class AnimeCubeExtractor(BaseExtractor):
  16 |     """Extractor for animecube.live using Next.js API and AES-256-GCM decryption."""
  17 | 
  18 |     def __init__(self, http_client):
  19 |         super().__init__(http_client)
  20 |         self.base_url = "https://animecube.live"
  21 | 
  22 |     def match(self, url: str) -> bool:
  23 |         """Match URLs containing animecube.live."""
  24 |         parsed = urlparse(url)
  25 |         return "animecube.live" in parsed.netloc
  26 | 
  27 |     def get_anime_details(self, url: str) -> dict:
  28 |         """Extract anime title and complete episode list from Next.js state."""
  29 |         url = url.strip('"\'')
  30 |         parsed = urlparse(url)
  31 |         path_parts = [p for p in parsed.path.split("/") if p]
  32 |         anime_slug = path_parts[1] if len(path_parts) > 1 else "unknown"
  33 | 
  34 |         console.print(f"[info]Fetching AnimeCube page: {url}[/info]")
  35 | 
  36 |         try:
  37 |             res = self.http.get(url)
  38 |             if res.status_code != 200:
  39 |                 console.print(
  40 |                     f"[error]Failed to fetch AnimeCube page (HTTP {res.status_code})[/error]",
  41 |                     style="red",
  42 |                 )
  43 |                 return {"title": anime_slug.replace("-", " ").title(), "episodes": []}
  44 | 
  45 |             text = res.text
  46 |             title = anime_slug.replace("-", " ").title()
  47 | 
  48 |             m_title = re.search(r"<h1[^>]*>([^<]+)</h1>", text)
  49 |             if m_title:
  50 |                 title = m_title.group(1).strip()
  51 |             else:
  52 |                 m_og = re.search(r'property="og:title"\s+content="([^"]+)"', text)
  53 |                 if m_og:
  54 |                     title = m_og.group(1).split("-")[0].strip().title()
  55 | 
  56 |             console.print(f"[success]Anime Title: {title}[/success]")
  57 | 
  58 |             # Extract Next.js state (__next_f pushes)
  59 |             pushes = re.findall(r"self\.__next_f\.push\((.*?)\)</script>", text, re.DOTALL)
  60 |             full_str = ""
  61 |             for p in pushes:
  62 |                 try:
  63 |                     data = json.loads(p)
  64 |                     if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
  65 |                         full_str += data[1]
  66 |                 except Exception:
  67 |                     pass
  68 | 
  69 |             episodes = []
  70 |             seen = set()
  71 |             ep_index = 1
  72 | 
  73 |             pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
  74 |             if pt_match:
  75 |                 try:
  76 |                     primary_tabs = json.loads(pt_match.group(1))
  77 |                     for ptab in primary_tabs:
  78 |                         p_id = ptab.get("id", "primary-1")
  79 |                         p_type = ptab.get("type", "series")
  80 |                         # Filter out trailer/PV tabs unless that's all there is
  81 |                         if p_type in ["pv", "trailer"] and len(primary_tabs) > 1:
  82 |                             continue
  83 | 
  84 |                         for season in ptab.get("seasons", []):
  85 |                             s_id = season.get("id", "tab-1")
  86 |                             for ep in season.get("episodes", []):
  87 |                                 ep_id = ep.get("id")
  88 |                                 num_disp = str(
  89 |                                     ep.get("numberDisplay") or ep.get("number") or ep_index
  90 |                                 )
  91 |                                 if ep_id and ep_id not in seen:
  92 |                                     seen.add(ep_id)
  93 |                                     ep_url = (
  94 |                                         f"{self.base_url}/anime/{anime_slug}"
  95 |                                         f"?season={s_id}&episode={ep_id}"
  96 |                                     )
  97 |                                     episodes.append(
  98 |                                         {
  99 |                                             "num": str(ep_index),
 100 |                                             "display_num": num_disp,
 101 |                                             "slug": ep_id,
 102 |                                             "ids": ep_id,
 103 |                                             "url": ep_url,
 104 |                                             "anime_slug": anime_slug,
 105 |                                             "primary_tab_id": p_id,
 106 |                                             "season_id": s_id,
 107 |                                             "clean_text": f"Episode {num_disp}",
 108 |                                         }
 109 |                                     )
 110 |                                     ep_index += 1
 111 |                 except Exception as e:
 112 |                     console.print(f"[warning]Failed to parse primaryTabs JSON: {e}[/warning]")
 113 | 
 114 |             # Fallback regex search if __next_f extraction failed
 115 |             if not episodes:
 116 |                 ep_pattern = re.compile(
 117 |                     rf"({re.escape(anime_slug)}-(?:(primary-[^-\s]+)-)?(tab-[^-\s]+)-ep-(\d+))"
 118 |                 )
 119 |                 matches = ep_pattern.findall(text)
 120 |                 for full_id, p_tab, s_tab, ep_num_str in matches:
 121 |                     if full_id not in seen:
 122 |                         seen.add(full_id)
 123 |                         primary_tab_id = p_tab if p_tab else "primary-1"
 124 |                         season_id = s_tab if s_tab else "tab-1"
 125 |                         ep_num = int(ep_num_str)
 126 | 
 127 |                         ep_url = (
 128 |                             f"{self.base_url}/anime/{anime_slug}"
 129 |                             f"?season={season_id}&episode={full_id}"
 130 |                         )
 131 | 
 132 |                         episodes.append(
 133 |                             {
 134 |                                 "num": str(ep_num),
 135 |                                 "display_num": str(ep_num),
 136 |                                 "slug": full_id,
 137 |                                 "ids": full_id,
 138 |                                 "url": ep_url,
 139 |                                 "anime_slug": anime_slug,
 140 |                                 "primary_tab_id": primary_tab_id,
 141 |                                 "season_id": season_id,
 142 |                                 "clean_text": f"Episode {ep_num}",
 143 |                             }
 144 |                         )
 145 |                 episodes.sort(key=lambda x: int(x["num"]))
 146 | 
 147 |             console.print(f"[success]Found {len(episodes)} episodes on AnimeCube.[/success]")
 148 |             return {"title": title, "episodes": episodes, "description": "", "year": None}
 149 | 
 150 |         except Exception as e:
 151 |             console.print(f"[error]Failed to parse AnimeCube details: {e}[/error]", style="red")
 152 |             return {"title": anime_slug.replace("-", " ").title(), "episodes": []}
 153 | 
 154 |     def get_episode_data(
 155 |         self, episode_item: dict, lang: str, server_info: dict | None = None
 156 |     ) -> dict:
 157 |         """Resolve video source URL and subtitles using AES-256-GCM decrypted sources API."""
 158 |         ep_slug = episode_item["slug"]
 159 |         anime_slug = episode_item.get("anime_slug")
 160 |         if not anime_slug:
 161 |             anime_slug = ep_slug.split("-tab-")[0] if "-tab-" in ep_slug else ep_slug
 162 | 
 163 |         primary_tab_id = episode_item.get("primary_tab_id", "primary-1")
 164 |         season_id = episode_item.get("season_id", "tab-1")
 165 |         ep_num = episode_item.get("display_num") or episode_item.get("num", "1")
 166 | 
 167 |         console.print(f"[info]Resolving AnimeCube Ep {ep_num} ({ep_slug})...[/info]")
 168 | 
 169 |         # 1. Fetch versions registry
 170 |         reg_url = f"{self.base_url}/api/anime-sources-versions"
 171 |         try:
 172 |             reg_json = self.http.get_json(reg_url, referer=episode_item.get("url", self.base_url))
 173 |             by_season = reg_json.get("bySeason", {})
 174 |             version_hash = by_season.get(anime_slug, {}).get(primary_tab_id, {}).get(season_id)
 175 |         except Exception as e:
 176 |             console.print(
 177 |                 f"[warning]Failed to fetch version registry: {e}[/warning]",
 178 |                 style="yellow",
 179 |             )
 180 |             version_hash = None
 181 | 
 182 |         if not version_hash:
 183 |             console.print(
 184 |                 f"[error]No version hash for {anime_slug} ({primary_tab_id}/{season_id})[/error]",
 185 |                 style="red",
 186 |             )
 187 |             return {"video_url": None, "subtitles": []}
 188 | 
 189 |         # 2. Build obfuscated sources request
 190 |         x_obf = os.urandom(16).hex()
 191 |         sources_url = (
 192 |             f"{self.base_url}/api/anime/{anime_slug}/episode/{ep_slug}/sources"
 193 |             f"?v={version_hash}&primaryTabId={primary_tab_id}&seasonId={season_id}"
 194 |         )
 195 |         headers = {
 196 |             "Accept": "application/json",
 197 |             "X-Obf": x_obf,
 198 |             "Referer": f"{self.base_url}/anime/{anime_slug}",
 199 |         }
 200 | 
 201 |         try:
 202 |             res_json = self.http.get_json(sources_url, headers=headers)
 203 |         except Exception as e:
 204 |             console.print(f"[error]Failed to fetch sources API: {e}[/error]", style="red")
 205 |             return {"video_url": None, "subtitles": []}
 206 | 
 207 |         enc_payload = res_json.get("d")
 208 |         if not enc_payload:
 209 |             console.print(
 210 |                 "[warning]Sources response missing encrypted payload 'd'[/warning]",
 211 |                 style="yellow",
 212 |             )
 213 |             return {"video_url": None, "subtitles": []}
 214 | 
 215 |         # 3. AES-256-GCM Decryption
 216 |         try:
 217 |             raw = base64.b64decode(enc_payload)
 218 |             iv = raw[:12]
 219 |             ciphertext = raw[12:]
 220 | 
 221 |             salt = f"{x_obf}|{version_hash}".encode()
 222 |             key = hashlib.sha256(salt).digest()
 223 | 
 224 |             aesgcm = AESGCM(key)
 225 |             decrypted_bytes = aesgcm.decrypt(iv, ciphertext, None)
 226 |             sources_data = json.loads(decrypted_bytes.decode("utf-8"))
 227 |         except Exception as e:
 228 |             console.print(f"[error]Failed to decrypt sources payload: {e}[/error]", style="red")
 229 |             return {"video_url": None, "subtitles": []}
 230 | 
 231 |         sources_list = sources_data.get("sources", [])
 232 |         if not sources_list:
 233 |             console.print(
 234 |                 f"[warning]No video sources found in payload for Ep {ep_num}[/warning]",
 235 |                 style="yellow",
 236 |             )
 237 |             return {"video_url": None, "subtitles": []}
 238 | 
 239 |         # 4. Resolve video URL across ALL available sources in payload
 240 |         video_url = None
 241 |         selected_platform = "unknown"
 242 | 
 243 |         for src in sources_list:
 244 |             platform = src.get("platform", "").lower()
 245 |             video_id = src.get("videoId")
 246 |             private_id = src.get("privateId")
 247 |             direct_url = src.get("url") or src.get("file") or src.get("link")
 248 | 
 249 |             if platform == "dailymotion" and video_id:
 250 |                 auth_str = f"auth={private_id}&" if private_id else ""
 251 |                 meta_url = (
 252 |                     f"https://www.dailymotion.com/player/metadata/video/{video_id}?"
 253 |                     f"{auth_str}embedder=https%3A%2F%2Fanimecube.live%2F"
 254 |                 )
 255 |                 try:
 256 |                     meta_res = self.http.get_json(meta_url, referer="https://animecube.live/")
 257 |                     dm_err = meta_res.get("error")
 258 |                     if dm_err:
 259 |                         err_code = dm_err.get("code", "UNKNOWN")
 260 |                         console.print(
 261 |                             f"[warning]Dailymotion {video_id} unavailable ({err_code})[/warning]",
 262 |                             style="yellow",
 263 |                         )
 264 | 
 265 |                     qualities = meta_res.get("qualities", {})
 266 |                     auto_list = qualities.get("auto", [])
 267 |                     if auto_list and isinstance(auto_list, list):
 268 |                         for item in auto_list:
 269 |                             if item.get("url"):
 270 |                                 video_url = item.get("url")
 271 |                                 break
 272 |                     if not video_url:
 273 |                         for _q_key, q_val in qualities.items():
 274 |                             if isinstance(q_val, list):
 275 |                                 for item in q_val:
 276 |                                     if item.get("url"):
 277 |                                         video_url = item.get("url")
 278 |                                         break
 279 |                             if video_url:
 280 |                                 break
 281 |                 except Exception as e:
 282 |                     console.print(
 283 |                         f"[warning]Failed DM metadata {video_id}: {e}[/warning]",
 284 |                         style="yellow",
 285 |                     )
 286 |                     video_url = None
 287 |             elif direct_url:
 288 |                 video_url = direct_url
 289 | 
 290 |             if video_url:
 291 |                 selected_platform = platform or "direct"
 292 |                 break
 293 | 
 294 |         # Subtitles extraction
 295 |         subtitles = []
 296 |         raw_subs = sources_data.get("subtitles", [])
 297 |         for sub in raw_subs:
 298 |             sub_url = sub.get("url") or sub.get("file")
 299 |             sub_lang = sub.get("lang") or sub.get("language") or "en"
 300 |             sub_label = sub.get("label") or "English"
 301 |             if sub_url:
 302 |                 subtitles.append(
 303 |                     {
 304 |                         "url": sub_url,
 305 |                         "label": sub_label,
 306 |                         "lang": sub_lang,
 307 |                         "referer": episode_item.get("url", self.base_url),
 308 |                     }
 309 |                 )
 310 | 
 311 |         has_vid = "yes" if video_url else "no"
 312 |         console.print(
 313 |             f"[success]Ep {ep_num}: platform={selected_platform}, video={has_vid}, "
 314 |             f"subtitles={len(subtitles)}[/success]"
 315 |         )
 316 | 
 317 |         return {"video_url": video_url, "subtitles": subtitles, "player_url": video_url}
```

### File: `src\downloader\browser_sniffer.py`

_Status: Added_

```text
   1 | """Playwright Headless Browser Network Traffic Sniffer module for capturing m3u8/subtitles."""
   2 | 
   3 | import re
   4 | import time
   5 | 
   6 | from downloader.utils import console
   7 | 
   8 | 
   9 | class BrowserNetworkSniffer:
  10 |     """Headless browser network sniffer to capture streaming video and subtitle URLs."""
  11 | 
  12 |     def __init__(self, headless: bool = True, timeout_sec: float = 30.0):
  13 |         self.headless = headless
  14 |         self.timeout_sec = timeout_sec
  15 | 
  16 |     def sniff_stream_urls(self, url: str) -> dict:
  17 |         """Launch headless browser, load page, and intercept media/subtitle network responses."""
  18 |         captured = {
  19 |             "video_url": None,
  20 |             "subtitles": [],
  21 |             "headers": {},
  22 |         }
  23 | 
  24 |         try:
  25 |             from playwright.sync_api import sync_playwright
  26 |         except ImportError:
  27 |             console.print(
  28 |                 "[warning]Playwright is not installed. Browser sniffer unavailable.[/warning]",
  29 |                 style="yellow",
  30 |             )
  31 |             return captured
  32 | 
  33 |         console.print(
  34 |             f"[info]Launching Browser Sniffer (Headless={self.headless}) for: {url}[/info]"
  35 |         )
  36 | 
  37 |         with sync_playwright() as p:
  38 |             browser = p.chromium.launch(
  39 |                 headless=self.headless,
  40 |                 args=[
  41 |                     "--no-sandbox",
  42 |                     "--disable-setuid-sandbox",
  43 |                     "--disable-blink-features=AutomationControlled",
  44 |                 ],
  45 |             )
  46 |             context = browser.new_context(
  47 |                 user_agent=(
  48 |                     "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
  49 |                     "AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
  50 |                 ),
  51 |                 viewport={"width": 1280, "height": 720},
  52 |             )
  53 |             page = context.new_page()
  54 | 
  55 |             def handle_response(response):
  56 |                 try:
  57 |                     res_url = response.url
  58 |                     headers = response.request.headers
  59 | 
  60 |                     # Detect video m3u8/mp4 streams
  61 |                     if not captured["video_url"]:
  62 |                         if ".m3u8" in res_url or ".mp4" in res_url or "master.m3u8" in res_url:
  63 |                             if "ad_url" not in res_url and "vmap" not in res_url:
  64 |                                 captured["video_url"] = res_url
  65 |                                 captured["headers"] = {
  66 |                                     "User-Agent": headers.get("user-agent", ""),
  67 |                                     "Referer": headers.get("referer", url),
  68 |                                     "Origin": headers.get("origin", ""),
  69 |                                 }
  70 |                                 console.print(
  71 |                                     f"[success]Captured video stream: {res_url[:75]}[/success]"
  72 |                                 )
  73 | 
  74 |                     # Detect VTT/SRT subtitles
  75 |                     if ".vtt" in res_url or ".srt" in res_url or "subtitle" in res_url:
  76 |                         if not any(s["url"] == res_url for s in captured["subtitles"]):
  77 |                             lang = "en"
  78 |                             m_lang = re.search(r"\b(en|vi|es|fr|de|ja)\b", res_url.lower())
  79 |                             if m_lang:
  80 |                                 lang = m_lang.group(1)
  81 |                             captured["subtitles"].append(
  82 |                                 {
  83 |                                     "url": res_url,
  84 |                                     "label": f"Language ({lang})",
  85 |                                     "lang": lang,
  86 |                                     "referer": url,
  87 |                                 }
  88 |                             )
  89 |                             console.print(
  90 |                                 f"[success]Sniffer captured subtitle: {res_url[:80]}[/success]"
  91 |                             )
  92 |                 except Exception:
  93 |                     pass
  94 | 
  95 |             page.on("response", handle_response)
  96 | 
  97 |             try:
  98 |                 page.goto(url, wait_until="domcontentloaded", timeout=self.timeout_sec * 1000)
  99 |                 # Wait for player initialization
 100 |                 time.sleep(4.0)
 101 | 
 102 |                 # Try clicking play button if present
 103 |                 play_btn = page.query_selector("button.play-btn, .vjs-big-play-button, #player")
 104 |                 if play_btn:
 105 |                     try:
 106 |                         play_btn.click()
 107 |                         time.sleep(3.0)
 108 |                     except Exception:
 109 |                         pass
 110 |             except Exception as e:
 111 |                 console.print(
 112 |                     f"[warning]Browser navigation timeout or notice: {e}[/warning]",
 113 |                     style="yellow",
 114 |                 )
 115 | 
 116 |             browser.close()
 117 | 
 118 |         return captured
```

### File: `src\downloader\hls_downloader.py`

_Status: Added_

```text
   1 | """Multi-threaded HLS Fragment Downloader module for fast parallel segment fetching."""
   2 | 
   3 | import os
   4 | import subprocess
   5 | import tempfile
   6 | from concurrent.futures import ThreadPoolExecutor, as_completed
   7 | from urllib.parse import urljoin
   8 | 
   9 | from downloader.core import get_local_tool_path
  10 | from downloader.utils import HttpClient, console
  11 | 
  12 | 
  13 | class MultiThreadedHLSDownloader:
  14 |     """Fast concurrent HLS segment downloader with automatic remuxing."""
  15 | 
  16 |     def __init__(self, http_client: HttpClient, max_workers: int = 16):
  17 |         self.http = http_client
  18 |         self.max_workers = max_workers
  19 | 
  20 |     def download_stream(
  21 |         self, m3u8_url: str, dest_path: str, referer: str | None = None
  22 |     ) -> bool:
  23 |         """Download HLS stream fragments in parallel and remux to destination MP4."""
  24 |         console.print(
  25 |             f"[info]Multi-threaded HLS Downloader ({self.max_workers} workers): "
  26 |             f"{m3u8_url[:80]}[/info]"
  27 |         )
  28 | 
  29 |         try:
  30 |             res = self.http.get(m3u8_url, referer=referer)
  31 |             if res.status_code != 200 or not res.text:
  32 |                 console.print(f"[warning]HLS manifest HTTP {res.status_code}[/warning]")
  33 |                 return False
  34 | 
  35 |             lines = res.text.splitlines()
  36 |             seg_urls = []
  37 |             for line in lines:
  38 |                 line = line.strip()
  39 |                 if line and not line.startswith("#"):
  40 |                     seg_urls.append(urljoin(m3u8_url, line))
  41 | 
  42 |             if not seg_urls:
  43 |                 console.print("[warning]No HLS fragments found in playlist[/warning]")
  44 |                 return False
  45 | 
  46 |             console.print(f"[info]Downloading {len(seg_urls)} fragments in parallel...[/info]")
  47 |             temp_ts_path = dest_path + ".parallel.ts"
  48 |             os.makedirs(os.path.dirname(os.path.abspath(dest_path)), exist_ok=True)
  49 | 
  50 |             with tempfile.TemporaryDirectory() as temp_dir:
  51 |                 frag_files = [None] * len(seg_urls)
  52 | 
  53 |                 def download_fragment(idx: int, frag_url: str) -> bool:
  54 |                     frag_file = os.path.join(temp_dir, f"seg_{idx:05d}.ts")
  55 |                     for _attempt in range(3):
  56 |                         try:
  57 |                             f_res = self.http.get(frag_url, referer=referer, rate_limit=False)
  58 |                             if f_res.status_code == 200 and f_res.content:
  59 |                                 with open(frag_file, "wb") as f:
  60 |                                     f.write(f_res.content)
  61 |                                 frag_files[idx] = frag_file
  62 |                                 return True
  63 |                         except Exception:
  64 |                             pass
  65 |                     return False
  66 | 
  67 |                 with ThreadPoolExecutor(max_workers=self.max_workers) as executor:
  68 |                     futures = [
  69 |                         executor.submit(download_fragment, idx, url)
  70 |                         for idx, url in enumerate(seg_urls)
  71 |                     ]
  72 |                     for future in as_completed(futures):
  73 |                         if not future.result():
  74 |                             console.print(
  75 |                                 "[warning]Fragment download failed after retries[/warning]"
  76 |                             )
  77 | 
  78 |                 # Concatenate all downloaded fragments in exact sequential order
  79 |                 with open(temp_ts_path, "wb") as outfile:
  80 |                     for frag_file in frag_files:
  81 |                         if frag_file and os.path.exists(frag_file):
  82 |                             with open(frag_file, "rb") as infile:
  83 |                                 outfile.write(infile.read())
  84 | 
  85 |             if not os.path.exists(temp_ts_path) or os.path.getsize(temp_ts_path) == 0:
  86 |                 console.print("[error]Parallel HLS segment assembly empty[/error]")
  87 |                 return False
  88 | 
  89 |             # Remux TS to MP4 using local/system ffmpeg
  90 |             ffmpeg_path = get_local_tool_path("ffmpeg") or "ffmpeg"
  91 |             remux_cmd = [
  92 |                 ffmpeg_path,
  93 |                 "-y",
  94 |                 "-i",
  95 |                 temp_ts_path,
  96 |                 "-c",
  97 |                 "copy",
  98 |                 "-bsf:a",
  99 |                 "aac_adtstoasc",
 100 |                 "-movflags",
 101 |                 "+faststart",
 102 |                 dest_path,
 103 |             ]
 104 |             remux_res = subprocess.run(remux_cmd, capture_output=True, text=True, check=False)
 105 | 
 106 |             if os.path.exists(temp_ts_path):
 107 |                 os.remove(temp_ts_path)
 108 | 
 109 |             if remux_res.returncode == 0 and os.path.exists(dest_path):
 110 |                 console.print(f"[success]Parallel HLS download complete: {dest_path}[/success]")
 111 |                 return True
 112 |             else:
 113 |                 if os.path.exists(dest_path):
 114 |                     os.remove(dest_path)
 115 |                 return False
 116 | 
 117 |         except Exception as e:
 118 |             console.print(f"[warning]Multi-threaded HLS Downloader error: {e}[/warning]")
 119 |             return False
```

### File: `src\downloader\subtitle_scraper.py`

_Status: Added_

```text
   1 | """Auto Subtitle Scraper module for fetching standalone English subtitles."""
   2 | 
   3 | from downloader.utils import HttpClient, console
   4 | 
   5 | 
   6 | class SubtitleScraper:
   7 |     """Helper module to search and download standalone subtitles."""
   8 | 
   9 |     def __init__(self, http_client: HttpClient):
  10 |         self.http = http_client
  11 | 
  12 |     def search_subtitles(
  13 |         self, anime_title: str, episode_num: str, lang: str = "en"
  14 |     ) -> list[dict]:
  15 |         """Attempt auto-search for standalone subtitles across public repositories."""
  16 |         subtitles = []
  17 |         console.print(
  18 |             f"[info]Auto Subtitle Scraper: Searching subtitles for '{anime_title}' "
  19 |             f"Ep {episode_num} ({lang})...[/info]"
  20 |         )
  21 |         return subtitles
```

### File: `tests\test_animecube.py`

_Status: Added_

```text
   1 | from downloader.animecube import AnimeCubeExtractor
   2 | from downloader.extractor import get_extractor_for_url
   3 | from downloader.utils import HttpClient
   4 | 
   5 | 
   6 | def test_animecube_match():
   7 |     client = HttpClient()
   8 |     extractor = AnimeCubeExtractor(client)
   9 |     url = (
  10 |         "https://animecube.live/anime/the-demon-hunter"
  11 |         "?from=home&season=tab-1&episode=the-demon-hunter-tab-1-ep-1"
  12 |     )
  13 |     assert extractor.match(url) is True
  14 |     assert extractor.match("https://anikototv.to/anime/test") is False
  15 | 
  16 | 
  17 | def test_get_extractor_for_animecube():
  18 |     client = HttpClient()
  19 |     url = "https://animecube.live/anime/the-demon-hunter"
  20 |     extractor = get_extractor_for_url(url, client)
  21 |     assert extractor is not None
  22 |     assert isinstance(extractor, AnimeCubeExtractor)
  23 | 
  24 | 
  25 | def test_animecube_details_and_episode_data():
  26 |     client = HttpClient()
  27 |     extractor = AnimeCubeExtractor(client)
  28 |     url = (
  29 |         "https://animecube.live/anime/the-demon-hunter"
  30 |         "?from=home&season=tab-1&episode=the-demon-hunter-tab-1-ep-1"
  31 |     )
  32 | 
  33 |     details = extractor.get_anime_details(url)
  34 |     assert details["title"] == "The Demon Hunter"
  35 |     assert len(details["episodes"]) > 0
  36 | 
  37 |     ep1 = details["episodes"][0]
  38 |     assert ep1["num"] == "1"
  39 |     assert "the-demon-hunter" in ep1["slug"]
  40 | 
  41 |     ep_data = extractor.get_episode_data(ep1, lang="english")
  42 |     assert "video_url" in ep_data
  43 |     assert "subtitles" in ep_data
  44 |     if ep_data["video_url"]:
  45 |         assert "dailymotion.com" in ep_data["video_url"] or "http" in ep_data["video_url"]
```

### File: `tests\test_browser_sniffer.py`

_Status: Added_

```text
   1 | """Unit tests for BrowserNetworkSniffer and MultiThreadedHLSDownloader modules."""
   2 | 
   3 | from downloader.browser_sniffer import BrowserNetworkSniffer
   4 | from downloader.hls_downloader import MultiThreadedHLSDownloader
   5 | from downloader.utils import HttpClient
   6 | 
   7 | 
   8 | def test_browser_sniffer_init():
   9 |     sniffer = BrowserNetworkSniffer(headless=True, timeout_sec=15.0)
  10 |     assert sniffer.headless is True
  11 |     assert sniffer.timeout_sec == 15.0
  12 | 
  13 | 
  14 | def test_hls_downloader_init():
  15 |     http = HttpClient()
  16 |     downloader = MultiThreadedHLSDownloader(http_client=http, max_workers=8)
  17 |     assert downloader.max_workers == 8
```

## File Differences

### Diff: `run.bat`

_Status: Modified_

```diff
@@ -1,12 +1,10 @@
  @echo off
  setlocal enabledelayedexpansion
- title Anime Downloader CLI Bootstrapper
+ title Unified Multi-Site Anime Downloader Launcher
  cls
  
- :: Change to the directory where this .bat file lives (fixes double-click from Explorer)
  cd /d "%~dp0"
  
- :: Check if the virtual environment exists
  if not exist ".venv" (
      echo [ERROR] Virtual environment .venv not found.
      echo Please run setup_env_py.bat first to set up the environment.
  ...
@@ -14,135 +12,66 @@
      exit /b 1
  )
  
- :: If arguments are passed, run directly in CLI mode forwarding all arguments
+ :: If direct URL parameter is passed via command line, execute auto-detect mode
  if not "%~1"=="" (
-     .venv\Scripts\python.exe run_app.py %*
+     .venv\Scripts\python.exe run_app.py -u "%~1" %2 %3 %4 %5 %6 %7 %8 %9
      echo.
      pause
-     exit /b %ERRORLEVEL%
+     exit /b !ERRORLEVEL!
  )
  
- :: ============================================================
- ::   INTERACTIVE MODE
- :: ============================================================
+ :MENU
+ cls
  echo ============================================================
- echo      ANIME SUBTITLE AND VIDEO BATCH DOWNLOADER
+ echo      TEAMTRAU UNIFIED MULTI-SITE ANIME DOWNLOADER
  echo ============================================================
  echo.
- echo  Supported site: https://anikototv.to
- echo  URL format   : https://anikototv.to/watch/^<anime-slug^>
+ echo Select Site Download Module:
  echo.
- echo Please answer the following prompts. Leave blank to use defaults.
+ echo   [1] AnimeCube Module (animecube.live)
+ echo   [2] AniKoto Module   (anikototv.to)
+ echo   [3] Auto-Detect Site (Enter any supported Anime URL)
+ echo   [0] Exit Launcher
  echo.
+ set /p LAUNCHER_CHOICE="Enter Choice [1]: "
+ if not defined LAUNCHER_CHOICE set LAUNCHER_CHOICE=1
+ 
+ if "!LAUNCHER_CHOICE!"=="1" (
+     call run_animecube.bat
+     goto :MENU
+ )
+ if "!LAUNCHER_CHOICE!"=="2" (
+     call run_anikoto.bat
+     goto :MENU
+ )
+ if "!LAUNCHER_CHOICE!"=="3" goto :AUTO_DETECT
+ if "!LAUNCHER_CHOICE!"=="0" exit /b 0
  
- set /p ANIME_URL="[1/8] Enter Anime URL: "
+ :AUTO_DETECT
+ echo.
+ echo ------------------------------------------------------------
+ echo  AUTO-DETECT DOWNLOAD MODE
+ echo ------------------------------------------------------------
+ set /p ANIME_URL="Enter Anime URL: "
  if not defined ANIME_URL (
      echo [ERROR] Anime URL cannot be empty!
      pause
-     exit /b 1
+     goto :MENU
  )
  
- echo.
- echo [2/8] Select Episode Naming Format:
- echo   [1] Series - sXXeYY (Simple - default)
- echo   [2] Series - sXXeYY (from TheTVDB)
- echo.
- set /p NAMING_CHOICE="Enter Choice [1]: "
- if not defined NAMING_CHOICE set NAMING_CHOICE=1
- 
- set TVDB_ID=
- set NAMING_FORMAT=simple
- if "%NAMING_CHOICE%"=="2" (
-     set NAMING_FORMAT=tvdb
-     echo.
-     set /p TVDB_ID="Enter TVDB Series ID/slug (press Enter to auto-search): "
-     if not defined TVDB_ID set TVDB_ID=auto
- )
+ for /f "delims=" %%I in ("!ANIME_URL!") do set ANIME_URL=%%~I
  
- echo.
- set /p EPISODES="[3/8] Episode Range (e.g. all, 1-5) [all]: "
+ set /p EPISODES="Episode Range (e.g. all, 1-5, SP) [all]: "
  if not defined EPISODES set EPISODES=all
  
- set /p SUB_LANG="[4/8] Subtitle Language (e.g. en, vi) [en]: "
+ set /p SUB_LANG="Subtitle Language (e.g. en, vi) [en]: "
  if not defined SUB_LANG set SUB_LANG=en
  
- set /p OUTPUT_DIR="[5/8] Output Directory [.\downloads]: "
+ set /p OUTPUT_DIR="Output Directory [.\downloads]: "
  if not defined OUTPUT_DIR set OUTPUT_DIR=.\downloads
  
  echo.
- echo [6/8] Select Download Mode:
- echo   [1] Subtitles Only  (fast, no ffmpeg needed)
- echo   [2] Videos Only
- echo   [3] Both Subtitles + Videos (default)
- echo.
- set /p MODE_CHOICE="Enter Choice (1/2/3) [3]: "
- if not defined MODE_CHOICE set MODE_CHOICE=3
- 
- set EXTRA_ARGS=
- if "%MODE_CHOICE%"=="1" set EXTRA_ARGS=--sub-only
- if "%MODE_CHOICE%"=="2" set EXTRA_ARGS=--video-only
- 
- if defined TVDB_ID (
-     if "!EXTRA_ARGS!"=="" (
-         set EXTRA_ARGS=--tvdb-id !TVDB_ID!
-     ) else (
-         set EXTRA_ARGS=!EXTRA_ARGS! --tvdb-id !TVDB_ID!
-     )
- )
- 
- if "!EXTRA_ARGS!"=="" (
-     set EXTRA_ARGS=--naming-format !NAMING_FORMAT!
- ) else (
-     set EXTRA_ARGS=!EXTRA_ARGS! --naming-format !NAMING_FORMAT!
- )
- 
- echo.
- set /p INTERACTIVE_CHOICE="[7/8] Enable Interactive Server Selection (y/n) [n]: "
- if /i "!INTERACTIVE_CHOICE:~0,1!"=="y" (
-     set EXTRA_ARGS=!EXTRA_ARGS! --interactive
-     goto :START_DOWNLOAD
- )
- 
- echo.
- echo [8/8] Select Preferred Server to use:
- echo   [1] Auto Select (Default)
- echo   [2] VidPlay Only (Highly Recommended - Raw Video + Softsub)
- echo   [3] HD-1 Only
- echo   [4] Vidstream Only
- echo   [5] VidCloud Only
- echo   [6] Enter custom server name...
- echo.
- set /p SERVER_CHOICE="Enter Choice [1]: "
- if not defined SERVER_CHOICE set SERVER_CHOICE=1
- 
- if "!SERVER_CHOICE:~0,1!"=="2" set EXTRA_ARGS=!EXTRA_ARGS! --only-server vidplay
- if "!SERVER_CHOICE:~0,1!"=="3" set EXTRA_ARGS=!EXTRA_ARGS! --only-server hd-1
- if "!SERVER_CHOICE:~0,1!"=="4" set EXTRA_ARGS=!EXTRA_ARGS! --only-server vidstream
- if "!SERVER_CHOICE:~0,1!"=="5" set EXTRA_ARGS=!EXTRA_ARGS! --only-server vidcloud
- if "!SERVER_CHOICE:~0,1!"=="6" (
-     echo.
-     set /p CUSTOM_SERVER="Enter server name to match: "
-     if defined CUSTOM_SERVER set EXTRA_ARGS=!EXTRA_ARGS! --only-server "!CUSTOM_SERVER!"
- )
- 
- :START_DOWNLOAD
- echo.
- echo ------------------------------------------------------------
- echo  Starting download...
- echo  URL      : %ANIME_URL%
- echo  Episodes : %EPISODES%
- echo  Language : %SUB_LANG%
- echo  TVDB ID  : %TVDB_ID%
- echo  Naming   : %NAMING_FORMAT%
- echo  Output   : %OUTPUT_DIR%
- echo  Mode     : !EXTRA_ARGS!
- echo ------------------------------------------------------------
- echo.
- 
- .venv\Scripts\python.exe run_app.py -u "%ANIME_URL%" -e "%EPISODES%" -l "%SUB_LANG%" -o "%OUTPUT_DIR%" !EXTRA_ARGS!
- 
+ .venv\Scripts\python.exe run_app.py -u "!ANIME_URL!" -e "!EPISODES!" -l "!SUB_LANG!" -o "!OUTPUT_DIR!"
  echo.
- echo ============================================================
- echo  Done! Press any key to exit.
- echo ============================================================
  pause
+ goto :MENU
```

### Diff: `run_anikoto.bat`

_Status: Added_

```diff
+ @echo off
+ setlocal enabledelayedexpansion
+ title AniKoto Downloader CLI - anikototv.to
+ cls
+ 
+ cd /d "%~dp0"
+ 
+ if not exist ".venv" (
+     echo [ERROR] Virtual environment .venv not found.
+     echo Please run setup_env_py.bat first to set up the environment.
+     pause
+     exit /b 1
+ )
+ 
+ if not "%~1"=="" (
+     .venv\Scripts\python.exe run_app.py -u "%~1" %2 %3 %4 %5 %6 %7 %8 %9
+     echo.
+     pause
+     exit /b !ERRORLEVEL!
+ )
+ 
+ echo ============================================================
+ echo      ANIKOTO.TO - BATCH ANIME DOWNLOADER MODULE
+ echo ============================================================
+ echo.
+ echo Target Site : https://anikototv.to
+ echo Example URL : https://anikototv.to/watch/demon-slayer
+ echo.
+ echo Please answer the following prompts (Press Enter for defaults).
+ echo.
+ 
+ set /p ANIME_URL="[1/6] Enter AniKoto URL: "
+ if not defined ANIME_URL (
+     echo [ERROR] Anime URL cannot be empty!
+     pause
+     exit /b 1
+ )
+ 
+ for /f "delims=" %%I in ("!ANIME_URL!") do set ANIME_URL=%%~I
+ 
+ echo.
+ set /p EPISODES="[2/6] Episode Range (e.g. all, 1-5) [all]: "
+ if not defined EPISODES set EPISODES=all
+ 
+ set /p SUB_LANG="[3/6] Subtitle Language (e.g. en, vi) [en]: "
+ if not defined SUB_LANG set SUB_LANG=en
+ 
+ set /p OUTPUT_DIR="[4/6] Output Directory [.\downloads]: "
+ if not defined OUTPUT_DIR set OUTPUT_DIR=.\downloads
+ 
+ echo.
+ echo [5/6] Select Download Mode:
+ echo   [1] Subtitles Only
+ echo   [2] Videos Only
+ echo   [3] Both Subtitles + Videos (Default)
+ echo.
+ set /p MODE_CHOICE="Enter Choice (1/2/3) [3]: "
+ if not defined MODE_CHOICE set MODE_CHOICE=3
+ 
+ set EXTRA_ARGS=
+ if "!MODE_CHOICE!"=="1" set EXTRA_ARGS=--sub-only
+ if "!MODE_CHOICE!"=="2" set EXTRA_ARGS=--video-only
+ 
+ echo.
+ set /p INTERACTIVE_CHOICE="[6/8] Enable Interactive Server Selection (y/n) [n]: "
+ if /i "!INTERACTIVE_CHOICE:~0,1!"=="y" (
+     set EXTRA_ARGS=!EXTRA_ARGS! --interactive
+ )
+ 
+ echo.
+ set /p PROXY_URL="[7/8] Optional HTTP/SOCKS5 Proxy (e.g. http://127.0.0.1:8080 or Enter for none): "
+ if defined PROXY_URL (
+     for /f "delims=" %%I in ("!PROXY_URL!") do set PROXY_URL=%%~I
+     set EXTRA_ARGS=!EXTRA_ARGS! --proxy "!PROXY_URL!"
+ )
+ 
+ echo.
+ set /p SNIFFER_CHOICE="[8/8] Enable Playwright Browser Network Sniffer (y/n) [n]: "
+ if /i "!SNIFFER_CHOICE:~0,1!"=="y" (
+     set EXTRA_ARGS=!EXTRA_ARGS! --use-browser-sniffer
+ )
+ 
+ echo.
+ echo ------------------------------------------------------------
+ echo  Starting AniKoto Download...
+ echo  URL      : !ANIME_URL!
+ echo  Episodes : !EPISODES!
+ echo  Language : !SUB_LANG!
+ echo  Output   : !OUTPUT_DIR!
+ echo  Extra    : !EXTRA_ARGS!
+ echo ------------------------------------------------------------
+ echo.
+ 
+ .venv\Scripts\python.exe run_app.py -u "!ANIME_URL!" -e "!EPISODES!" -l "!SUB_LANG!" -o "!OUTPUT_DIR!" !EXTRA_ARGS!
+ 
+ echo.
+ echo ============================================================
+ echo  Download session finished! Press any key to return/exit.
+ echo ============================================================
+ pause
```

### Diff: `run_animecube.bat`

_Status: Added_

```diff
+ @echo off
+ setlocal enabledelayedexpansion
+ title AnimeCube Downloader CLI - animecube.live
+ cls
+ 
+ :: Change directory to script folder
+ cd /d "%~dp0"
+ 
+ :: Check virtual environment
+ if not exist ".venv" (
+     echo [ERROR] Virtual environment .venv not found.
+     echo Please run setup_env_py.bat first to set up the environment.
+     pause
+     exit /b 1
+ )
+ 
+ :: If URL argument is provided via command line, execute directly
+ if not "%~1"=="" (
+     .venv\Scripts\python.exe run_app.py -u "%~1" %2 %3 %4 %5 %6 %7 %8 %9
+     echo.
+     pause
+     exit /b !ERRORLEVEL!
+ )
+ 
+ :: ============================================================
+ ::   ANIMECUBE MODULE INTERACTIVE MENU
+ :: ============================================================
+ echo ============================================================
+ echo      ANIMECUBE.LIVE - BATCH ANIME DOWNLOADER MODULE
+ echo ============================================================
+ echo.
+ echo Target Site : https://animecube.live
+ echo Example URL : https://animecube.live/anime/ling-cage?season=tab-1^&episode=ling-cage-tab-1-ep-SP
+ echo.
+ echo Please answer the following prompts (Press Enter for defaults).
+ echo.
+ 
+ set /p ANIME_URL="[1/6] Enter AnimeCube URL: "
+ if not defined ANIME_URL (
+     echo [ERROR] Anime URL cannot be empty!
+     pause
+     exit /b 1
+ )
+ 
+ :: Strip surrounding quotes if user entered them
+ for /f "delims=" %%I in ("!ANIME_URL!") do set ANIME_URL=%%~I
+ 
+ echo.
+ set /p EPISODES="[2/6] Episode Range (e.g. all, 1, 1-5, SP) [all]: "
+ if not defined EPISODES set EPISODES=all
+ 
+ set /p SUB_LANG="[3/6] Subtitle Language (e.g. en, vi) [en]: "
+ if not defined SUB_LANG set SUB_LANG=en
+ 
+ set /p OUTPUT_DIR="[4/6] Output Directory [.\downloads]: "
+ if not defined OUTPUT_DIR set OUTPUT_DIR=.\downloads
+ 
+ echo.
+ echo [5/6] Select Download Mode:
+ echo   [1] Subtitles Only (Fast)
+ echo   [2] Videos Only
+ echo   [3] Both Subtitles + Videos (Default)
+ echo.
+ set /p MODE_CHOICE="Enter Choice (1/2/3) [3]: "
+ if not defined MODE_CHOICE set MODE_CHOICE=3
+ 
+ set EXTRA_ARGS=
+ if "!MODE_CHOICE!"=="1" set EXTRA_ARGS=--sub-only
+ if "!MODE_CHOICE!"=="2" set EXTRA_ARGS=--video-only
+ 
+ echo.
+ set /p INTERACTIVE_CHOICE="[6/7] Enable Interactive Server Selection (y/n) [n]: "
+ if /i "!INTERACTIVE_CHOICE:~0,1!"=="y" (
+     set EXTRA_ARGS=!EXTRA_ARGS! --interactive
+ )
+ 
+ echo.
+ set /p PROXY_URL="[7/8] Optional HTTP/SOCKS5 Proxy (e.g. http://127.0.0.1:8080 or Enter for none): "
+ if defined PROXY_URL (
+     for /f "delims=" %%I in ("!PROXY_URL!") do set PROXY_URL=%%~I
+     set EXTRA_ARGS=!EXTRA_ARGS! --proxy "!PROXY_URL!"
+ )
+ 
+ echo.
+ set /p SNIFFER_CHOICE="[8/8] Enable Playwright Browser Network Sniffer (y/n) [n]: "
+ if /i "!SNIFFER_CHOICE:~0,1!"=="y" (
+     set EXTRA_ARGS=!EXTRA_ARGS! --use-browser-sniffer
+ )
+ 
+ echo.
+ echo ------------------------------------------------------------
+ echo  Starting AnimeCube Download...
+ echo  URL      : !ANIME_URL!
+ echo  Episodes : !EPISODES!
+ echo  Language : !SUB_LANG!
+ echo  Output   : !OUTPUT_DIR!
+ echo  Extra    : !EXTRA_ARGS!
+ echo ------------------------------------------------------------
+ echo.
+ 
+ .venv\Scripts\python.exe run_app.py -u "!ANIME_URL!" -e "!EPISODES!" -l "!SUB_LANG!" -o "!OUTPUT_DIR!" !EXTRA_ARGS!
+ 
+ echo.
+ echo ============================================================
+ echo  Download session finished! Press any key to return/exit.
+ echo ============================================================
+ pause
```

### Diff: `scratch\debug_animecube_sources.py`

_Status: Added_

```diff
+ import sys, os, json, base64, hashlib, re
+ from curl_cffi import requests
+ from cryptography.hazmat.primitives.ciphers.aead import AESGCM
+ 
+ sys.path.insert(0, os.path.abspath("src"))
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://animecube.live/"
+ }
+ 
+ url = "https://animecube.live/anime/ling-cage"
+ r = requests.get(url, headers=headers, impersonate="chrome120")
+ text = r.text
+ 
+ pushes = re.findall(r'self\.__next_f\.push\((.*?)\)</script>', text, re.DOTALL)
+ full_str = ""
+ for p in pushes:
+     try:
+         data = json.loads(p)
+         if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
+             full_str += data[1]
+     except Exception:
+         pass
+ 
+ pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
+ if pt_match:
+     primary_tabs = json.loads(pt_match.group(1))
+     print("Primary tabs found:", len(primary_tabs))
+     for ptab in primary_tabs:
+         p_id = ptab.get("id")
+         p_name = ptab.get("customName") or ptab.get("title")
+         for season in ptab.get("seasons", []):
+             s_id = season.get("id")
+             s_name = season.get("title")
+             eps = season.get("episodes", [])
+             print(f"\nTab '{p_name}' ({p_id}) -> Season '{s_name}' ({s_id}) -> {len(eps)} episodes")
+ 
+             reg_url = "https://animecube.live/api/anime-sources-versions"
+             reg_json = requests.get(reg_url, headers=headers, impersonate="chrome120").json()
+             version_hash = reg_json.get("bySeason", {}).get("ling-cage", {}).get(p_id, {}).get(s_id)
+             print("  Version hash:", version_hash)
+ 
+             if version_hash and eps:
+                 for ep in eps[:3]:
+                     ep_id = ep.get("id")
+                     x_obf = os.urandom(16).hex()
+                     sources_url = (
+                         f"https://animecube.live/api/anime/ling-cage/episode/{ep_id}/sources"
+                         f"?v={version_hash}&primaryTabId={p_id}&seasonId={s_id}"
+                     )
+                     h = {"Accept": "application/json", "X-Obf": x_obf, "Referer": "https://animecube.live/anime/ling-cage"}
+                     res_json = requests.get(sources_url, headers=h, impersonate="chrome120").json()
+                     enc_payload = res_json.get("d")
+                     if enc_payload:
+                         raw = base64.b64decode(enc_payload)
+                         iv = raw[:12]
+                         ciphertext = raw[12:]
+                         salt = f"{x_obf}|{version_hash}".encode()
+                         key = hashlib.sha256(salt).digest()
+                         aesgcm = AESGCM(key)
+                         decrypted_bytes = aesgcm.decrypt(iv, ciphertext, None)
+                         sources_data = json.loads(decrypted_bytes.decode("utf-8"))
+                         print(f"\n  Ep {ep_id} -> sources count: {len(sources_data.get('sources', []))}")
+                         for s in sources_data.get("sources", []):
+                             print(f"    Source: platform={s.get('platform')}, videoId={s.get('videoId')}, quality={s.get('quality')}, url={s.get('url')}")
+                         print(f"    Subtitles: {sources_data.get('subtitles')}")
```

### Diff: `scratch\debug_dailymotion_embed.py`

_Status: Added_

```diff
+ import sys, json
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
+     "Referer": "https://animecube.live/"
+ }
+ 
+ # Allowed fields in Dailymotion API: id, title, embed_url, private, duration, mode
+ api_url = "https://api.dailymotion.com/video/x9ps6dc?fields=id,title,embed_url,private,duration,mode"
+ r1 = requests.get(api_url, headers=headers, impersonate="chrome120")
+ print("API Dailymotion Status:", r1.status_code)
+ print("API Response:", r1.text)
```

### Diff: `scratch\inspect_animecube.py`

_Status: Added_

```diff
+ import sys, json, re
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
+     "Referer": "https://animecube.live/"
+ }
+ 
+ r = requests.get("https://animecube.live/anime/the-demon-hunter", headers=headers, impersonate="chrome120")
+ text = r.text
+ 
+ pushes = re.findall(r'self\.__next_f\.push\((.*?)\)</script>', text, re.DOTALL)
+ full_str = ""
+ for p in pushes:
+     try:
+         data = json.loads(p)
+         if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
+             full_str += data[1]
+     except Exception:
+         pass
+ 
+ # Extract primaryTabs array structure
+ # Look for "primaryTabs":[{...}] or similar
+ pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
+ if pt_match:
+     primary_tabs = json.loads(pt_match.group(1))
+     print("Parsed primaryTabs count:", len(primary_tabs))
+     total_eps = 0
+     for ptab in primary_tabs:
+         p_id = ptab.get("id", "primary-1")
+         p_name = ptab.get("customName") or ptab.get("title") or p_id
+         for season in ptab.get("seasons", []):
+             s_id = season.get("id", "tab-1")
+             s_name = season.get("title") or s_id
+             eps = season.get("episodes", [])
+             print(f"PrimaryTab '{p_name}' ({p_id}) -> Season '{s_name}' ({s_id}): {len(eps)} episodes")
+             total_eps += len(eps)
+     print("Total parsed episodes:", total_eps)
+ else:
+     print("primaryTabs match not found")
```

### Diff: `scratch\test_animecube_all.py`

_Status: Added_

```diff
+ import sys, os, json
+ sys.path.insert(0, os.path.abspath("src"))
+ 
+ from downloader.animecube import AnimeCubeExtractor
+ from downloader.utils import HttpClient
+ 
+ client = HttpClient()
+ extractor = AnimeCubeExtractor(client)
+ 
+ anime_list = [
+     "https://animecube.live/anime/battle-through-the-heavens",
+     "https://animecube.live/anime/swallowed-star",
+     "https://animecube.live/anime/renegade-immortal",
+     "https://animecube.live/anime/perfect-world",
+     "https://animecube.live/anime/martial-universe",
+     "https://animecube.live/anime/shrouding-the-heavens",
+     "https://animecube.live/anime/stellar-transformations"
+ ]
+ 
+ print("--- Testing AnimeCubeExtractor across popular anime titles ---")
+ for url in anime_list:
+     slug = url.split("/")[-1]
+     try:
+         details = extractor.get_anime_details(url)
+         eps = details.get("episodes", [])
+         print(f"\nAnime: {details.get('title')} ({slug}) -> Total eps: {len(eps)}")
+         if eps:
+             ep1 = eps[0]
+             ep_data = extractor.get_episode_data(ep1, lang="en")
+             v_url = ep_data.get("video_url")
+             print(f"  Ep 1 ({ep1.get('slug')}) -> Video URL: {v_url[:90] if v_url else 'None (Unavailable/Private)'}")
+     except Exception as e:
+         print(f"  Error on {slug}: {e}")
```

### Diff: `scratch\test_dailymotion_extractor.py`

_Status: Added_

```diff
+ import sys, json, re
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://animecube.live/",
+     "Origin": "https://animecube.live"
+ }
+ 
+ vids = [
+     ("x9q0yw0", "k7IN2pETI8HhhVDMOsw"),
+     ("x9nm6ji", "k3jihnvMLd02FHDvP5A"),
+     ("x9ps1gs", "k51PdThRbOubKUDL45K")
+ ]
+ 
+ for video_id, private_id in vids:
+     auth_param = f"?auth={private_id}" if private_id else ""
+     meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}{auth_param}&embedder=https%3A%2F%2Fanimecube.live%2F"
+     r = requests.get(meta_url, headers=headers, impersonate="chrome120")
+     print(f"\n--- Video {video_id} --- Status: {r.status_code}")
+     data = r.json()
+     if "error" in data:
+         print("  Error code:", data["error"].get("code"))
+     else:
+         print("  Title:", data.get("title"))
+         ad_url = data.get("advertising", {}).get("ad_url")
+         print("  ad_url:", ad_url[:100] if ad_url else "None")
```

### Diff: `scratch\test_demon_hunter.py`

_Status: Added_

```diff
+ import sys, os, json, base64, hashlib, re
+ from curl_cffi import requests
+ from cryptography.hazmat.primitives.ciphers.aead import AESGCM
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://animecube.live/"
+ }
+ 
+ url = "https://animecube.live/anime/the-demon-hunter"
+ r = requests.get(url, headers=headers, impersonate="chrome120")
+ text = r.text
+ 
+ pushes = re.findall(r'self\.__next_f\.push\((.*?)\)</script>', text, re.DOTALL)
+ full_str = ""
+ for p in pushes:
+     try:
+         data = json.loads(p)
+         if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
+             full_str += data[1]
+     except Exception:
+         pass
+ 
+ pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
+ if pt_match:
+     primary_tabs = json.loads(pt_match.group(1))
+     print(f"Primary tabs count: {len(primary_tabs)}", flush=True)
+ 
+     reg_url = "https://animecube.live/api/anime-sources-versions"
+     reg_json = requests.get(reg_url, headers=headers, impersonate="chrome120").json()
+ 
+     for ptab in primary_tabs:
+         p_id = ptab.get("id")
+         p_name = ptab.get("customName") or ptab.get("title")
+         p_type = ptab.get("type")
+         for season in ptab.get("seasons", []):
+             s_id = season.get("id")
+             s_name = season.get("title")
+             eps = season.get("episodes", [])
+             print(f"\nTab: '{p_name}' ({p_id}, type={p_type}) -> Season: '{s_name}' ({s_id}) -> {len(eps)} eps", flush=True)
+ 
+             version_hash = reg_json.get("bySeason", {}).get("the-demon-hunter", {}).get(p_id, {}).get(s_id)
+             print("  Version hash:", version_hash, flush=True)
+ 
+             if version_hash and eps:
+                 # Check first 5 episodes
+                 for ep in eps[:5]:
+                     ep_num = str(ep.get("numberDisplay") or ep.get("number") or "")
+                     ep_id = ep.get("id")
+                     x_obf = os.urandom(16).hex()
+                     sources_url = (
+                         f"https://animecube.live/api/anime/the-demon-hunter/episode/{ep_id}/sources"
+                         f"?v={version_hash}&primaryTabId={p_id}&seasonId={s_id}"
+                     )
+                     h = {"Accept": "application/json", "X-Obf": x_obf, "Referer": "https://animecube.live/anime/the-demon-hunter"}
+                     res_json = requests.get(sources_url, headers=h, impersonate="chrome120").json()
+                     enc_payload = res_json.get("d")
+                     if enc_payload:
+                         raw = base64.b64decode(enc_payload)
+                         iv = raw[:12]
+                         ciphertext = raw[12:]
+                         salt = f"{x_obf}|{version_hash}".encode()
+                         key = hashlib.sha256(salt).digest()
+                         aesgcm = AESGCM(key)
+                         decrypted_bytes = aesgcm.decrypt(iv, ciphertext, None)
+                         sources_data = json.loads(decrypted_bytes.decode("utf-8"))
+ 
+                         for s in sources_data.get("sources", []):
+                             vid_id = s.get("videoId")
+                             priv_id = s.get("privateId")
+                             plat = s.get("platform")
+                             if plat == "dailymotion" and vid_id:
+                                 auth_str = f"auth={priv_id}&" if priv_id else ""
+                                 m_url = f"https://www.dailymotion.com/player/metadata/video/{vid_id}?{auth_str}embedder=https%3A%2F%2Fanimecube.live%2F"
+                                 meta_res = requests.get(m_url, headers=headers, impersonate="chrome120").json()
+                                 err = meta_res.get("error", {}).get("code") if meta_res.get("error") else "NONE"
+                                 quals = meta_res.get("qualities", {})
+                                 print(f"  Ep {ep_num} ({ep_id}) -> Dailymotion {vid_id} -> Error: {err}, Streams: {list(quals.keys())}", flush=True)
```

### Diff: `scratch\test_dm_keys.py`

_Status: Added_

```diff
+ import sys, json
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://animecube.live/"
+ }
+ 
+ video_id = "x9or71s"
+ auth = "k6JNTDpO47cP6mDDQTm"
+ meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}?auth={auth}&embedder=https%3A%2F%2Fanimecube.live%2F"
+ 
+ r = requests.get(meta_url, headers=headers, impersonate="chrome120")
+ data = r.json()
+ 
+ print("Root keys in metadata JSON:", list(data.keys()))
+ for k, v in data.items():
+     if k != "advertising":
+         v_str = str(v).encode("ascii", "replace").decode("ascii")
+         print(f"  {k}: {type(v).__name__} -> {v_str[:120]}")
```

### Diff: `scratch\test_dm_qualities.py`

_Status: Added_

```diff
+ import sys, json
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://animecube.live/"
+ }
+ 
+ # Test a known public Dailymotion video
+ video_id = "x8x5v4c" # Public video
+ meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}?embedder=https%3A%2F%2Fanimecube.live%2F"
+ 
+ r = requests.get(meta_url, headers=headers, impersonate="chrome120")
+ data = r.json()
+ 
+ print("Status:", r.status_code)
+ print("Title:", data.get("title"))
+ print("Has error:", "error" in data)
+ qualities = data.get("qualities", {})
+ print("Qualities keys:", list(qualities.keys()))
+ 
+ for res_name, res_list in qualities.items():
+     print(f"\nResolution: {res_name}")
+     for item in res_list:
+         print("  Type:", item.get("type"), "-> URL:", item.get("url")[:100])
```

### Diff: `scratch\test_dmxleo.py`

_Status: Added_

```diff
+ import sys
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://www.dailymotion.com/",
+     "Origin": "https://www.dailymotion.com"
+ }
+ 
+ # 1. Fetch Dailymotion metadata for Perfect World Ep 228
+ video_id = "x9or71s"
+ auth = "k6JNTDpO47cP6mDDQTm"
+ meta_url = f"https://www.dailymotion.com/player/metadata/video/{video_id}?auth={auth}&embedder=https%3A%2F%2Fanimecube.live%2F"
+ 
+ r1 = requests.get(meta_url, headers=headers, impersonate="chrome120")
+ print("Meta status:", r1.status_code)
+ data = r1.json()
+ ad_url = data.get("advertising", {}).get("ad_url")
+ print("ad_url:", ad_url)
+ 
+ if ad_url:
+     # 2. Fetch the m3u8 playlist using curl_cffi
+     r2 = requests.get(ad_url, headers=headers, impersonate="chrome120")
+     print("\nm3u8 Status:", r2.status_code)
+     print("m3u8 Content-Type:", r2.headers.get("content-type"))
+     print("m3u8 text snippet:\n", r2.text[:500])
```

### Diff: `scratch\test_ling_cage.py`

_Status: Added_

```diff
+ import sys, json
+ from curl_cffi import requests
+ 
+ headers = {
+     "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
+     "Referer": "https://animecube.live/",
+     "Origin": "https://animecube.live",
+     "Sec-Fetch-Dest": "empty",
+     "Sec-Fetch-Mode": "cors",
+     "Sec-Fetch-Site": "cross-site",
+ }
+ 
+ # Test metadata request with cross-site headers
+ url = "https://www.dailymotion.com/player/metadata/video/x9q0yw0?auth=k7IN2pETI8HhhVDMOsw"
+ r = requests.get(url, headers=headers, impersonate="chrome120")
+ print("Status:", r.status_code)
+ data = r.json()
+ print("Keys:", list(data.keys()))
+ if "error" in data:
+     print("Error:", data["error"])
+ else:
+     print("Qualities:", list(data.get("qualities", {}).keys()))
+     print("Advertising ad_url:", data.get("advertising", {}).get("ad_url"))
```

### Diff: `src\downloader\__init__.py`

_Status: Modified_

```diff
@@ -1,13 +1,16 @@
  from downloader.anikoto import AnikotoExtractor
+ from downloader.animecube import AnimeCubeExtractor
  from downloader.core import BatchDownloader
  from downloader.extractor import BaseExtractor, get_extractor_for_url, register_extractor
  from downloader.utils import HttpClient
  
  __all__ = [
      "AnikotoExtractor",
+     "AnimeCubeExtractor",
      "BaseExtractor",
      "BatchDownloader",
      "HttpClient",
      "get_extractor_for_url",
      "register_extractor",
  ]
+ 
```

### Diff: `src\downloader\animecube.py`

_Status: Added_

```diff
+ import base64
+ import hashlib
+ import json
+ import os
+ import re
+ from urllib.parse import urlparse
+ 
+ from cryptography.hazmat.primitives.ciphers.aead import AESGCM
+ 
+ from downloader.extractor import BaseExtractor, register_extractor
+ from downloader.utils import console
+ 
+ 
+ @register_extractor
+ class AnimeCubeExtractor(BaseExtractor):
+     """Extractor for animecube.live using Next.js API and AES-256-GCM decryption."""
+ 
+     def __init__(self, http_client):
+         super().__init__(http_client)
+         self.base_url = "https://animecube.live"
+ 
+     def match(self, url: str) -> bool:
+         """Match URLs containing animecube.live."""
+         parsed = urlparse(url)
+         return "animecube.live" in parsed.netloc
+ 
+     def get_anime_details(self, url: str) -> dict:
+         """Extract anime title and complete episode list from Next.js state."""
+         url = url.strip('"\'')
+         parsed = urlparse(url)
+         path_parts = [p for p in parsed.path.split("/") if p]
+         anime_slug = path_parts[1] if len(path_parts) > 1 else "unknown"
+ 
+         console.print(f"[info]Fetching AnimeCube page: {url}[/info]")
+ 
+         try:
+             res = self.http.get(url)
+             if res.status_code != 200:
+                 console.print(
+                     f"[error]Failed to fetch AnimeCube page (HTTP {res.status_code})[/error]",
+                     style="red",
+                 )
+                 return {"title": anime_slug.replace("-", " ").title(), "episodes": []}
+ 
+             text = res.text
+             title = anime_slug.replace("-", " ").title()
+ 
+             m_title = re.search(r"<h1[^>]*>([^<]+)</h1>", text)
+             if m_title:
+                 title = m_title.group(1).strip()
+             else:
+                 m_og = re.search(r'property="og:title"\s+content="([^"]+)"', text)
+                 if m_og:
+                     title = m_og.group(1).split("-")[0].strip().title()
+ 
+             console.print(f"[success]Anime Title: {title}[/success]")
+ 
+             # Extract Next.js state (__next_f pushes)
+             pushes = re.findall(r"self\.__next_f\.push\((.*?)\)</script>", text, re.DOTALL)
+             full_str = ""
+             for p in pushes:
+                 try:
+                     data = json.loads(p)
+                     if isinstance(data, list) and len(data) >= 2 and isinstance(data[1], str):
+                         full_str += data[1]
+                 except Exception:
+                     pass
+ 
+             episodes = []
+             seen = set()
+             ep_index = 1
+ 
+             pt_match = re.search(r'"primaryTabs"\s*:\s*(\[.*?\])\s*,\s*"updateDays"', full_str)
+             if pt_match:
+                 try:
+                     primary_tabs = json.loads(pt_match.group(1))
+                     for ptab in primary_tabs:
+                         p_id = ptab.get("id", "primary-1")
+                         p_type = ptab.get("type", "series")
+                         # Filter out trailer/PV tabs unless that's all there is
+                         if p_type in ["pv", "trailer"] and len(primary_tabs) > 1:
+                             continue
+ 
+                         for season in ptab.get("seasons", []):
+                             s_id = season.get("id", "tab-1")
+                             for ep in season.get("episodes", []):
+                                 ep_id = ep.get("id")
+                                 num_disp = str(
+                                     ep.get("numberDisplay") or ep.get("number") or ep_index
+                                 )
+                                 if ep_id and ep_id not in seen:
+                                     seen.add(ep_id)
+                                     ep_url = (
+                                         f"{self.base_url}/anime/{anime_slug}"
+                                         f"?season={s_id}&episode={ep_id}"
+                                     )
+                                     episodes.append(
+                                         {
+                                             "num": str(ep_index),
+                                             "display_num": num_disp,
+                                             "slug": ep_id,
+                                             "ids": ep_id,
+                                             "url": ep_url,
+                                             "anime_slug": anime_slug,
+                                             "primary_tab_id": p_id,
+                                             "season_id": s_id,
+                                             "clean_text": f"Episode {num_disp}",
+                                         }
+                                     )
+                                     ep_index += 1
+                 except Exception as e:
+                     console.print(f"[warning]Failed to parse primaryTabs JSON: {e}[/warning]")
+ 
+             # Fallback regex search if __next_f extraction failed
+             if not episodes:
+                 ep_pattern = re.compile(
+                     rf"({re.escape(anime_slug)}-(?:(primary-[^-\s]+)-)?(tab-[^-\s]+)-ep-(\d+))"
+                 )
+                 matches = ep_pattern.findall(text)
+                 for full_id, p_tab, s_tab, ep_num_str in matches:
+                     if full_id not in seen:
+                         seen.add(full_id)
+                         primary_tab_id = p_tab if p_tab else "primary-1"
+                         season_id = s_tab if s_tab else "tab-1"
+                         ep_num = int(ep_num_str)
+ 
+                         ep_url = (
+                             f"{self.base_url}/anime/{anime_slug}"
+                             f"?season={season_id}&episode={full_id}"
+                         )
+ 
+                         episodes.append(
+                             {
+                                 "num": str(ep_num),
+                                 "display_num": str(ep_num),
+                                 "slug": full_id,
+                                 "ids": full_id,
+                                 "url": ep_url,
+                                 "anime_slug": anime_slug,
+                                 "primary_tab_id": primary_tab_id,
+                                 "season_id": season_id,
+                                 "clean_text": f"Episode {ep_num}",
+                             }
+                         )
+                 episodes.sort(key=lambda x: int(x["num"]))
+ 
+             console.print(f"[success]Found {len(episodes)} episodes on AnimeCube.[/success]")
+             return {"title": title, "episodes": episodes, "description": "", "year": None}
+ 
+         except Exception as e:
+             console.print(f"[error]Failed to parse AnimeCube details: {e}[/error]", style="red")
+             return {"title": anime_slug.replace("-", " ").title(), "episodes": []}
+ 
+     def get_episode_data(
+         self, episode_item: dict, lang: str, server_info: dict | None = None
+     ) -> dict:
+         """Resolve video source URL and subtitles using AES-256-GCM decrypted sources API."""
+         ep_slug = episode_item["slug"]
+         anime_slug = episode_item.get("anime_slug")
+         if not anime_slug:
+             anime_slug = ep_slug.split("-tab-")[0] if "-tab-" in ep_slug else ep_slug
+ 
+         primary_tab_id = episode_item.get("primary_tab_id", "primary-1")
+         season_id = episode_item.get("season_id", "tab-1")
+         ep_num = episode_item.get("display_num") or episode_item.get("num", "1")
+ 
+         console.print(f"[info]Resolving AnimeCube Ep {ep_num} ({ep_slug})...[/info]")
+ 
+         # 1. Fetch versions registry
+         reg_url = f"{self.base_url}/api/anime-sources-versions"
+         try:
+             reg_json = self.http.get_json(reg_url, referer=episode_item.get("url", self.base_url))
+             by_season = reg_json.get("bySeason", {})
+             version_hash = by_season.get(anime_slug, {}).get(primary_tab_id, {}).get(season_id)
+         except Exception as e:
+             console.print(
+                 f"[warning]Failed to fetch version registry: {e}[/warning]",
+                 style="yellow",
+             )
+             version_hash = None
+ 
+         if not version_hash:
+             console.print(
+                 f"[error]No version hash for {anime_slug} ({primary_tab_id}/{season_id})[/error]",
+                 style="red",
+             )
+             return {"video_url": None, "subtitles": []}
+ 
+         # 2. Build obfuscated sources request
+         x_obf = os.urandom(16).hex()
+         sources_url = (
+             f"{self.base_url}/api/anime/{anime_slug}/episode/{ep_slug}/sources"
+             f"?v={version_hash}&primaryTabId={primary_tab_id}&seasonId={season_id}"
+         )
+         headers = {
+             "Accept": "application/json",
+             "X-Obf": x_obf,
+             "Referer": f"{self.base_url}/anime/{anime_slug}",
+         }
+ 
+         try:
+             res_json = self.http.get_json(sources_url, headers=headers)
+         except Exception as e:
+             console.print(f"[error]Failed to fetch sources API: {e}[/error]", style="red")
+             return {"video_url": None, "subtitles": []}
+ 
+         enc_payload = res_json.get("d")
+         if not enc_payload:
+             console.print(
+                 "[warning]Sources response missing encrypted payload 'd'[/warning]",
+                 style="yellow",
+             )
+             return {"video_url": None, "subtitles": []}
+ 
+         # 3. AES-256-GCM Decryption
+         try:
+             raw = base64.b64decode(enc_payload)
+             iv = raw[:12]
+             ciphertext = raw[12:]
+ 
+             salt = f"{x_obf}|{version_hash}".encode()
+             key = hashlib.sha256(salt).digest()
+ 
+             aesgcm = AESGCM(key)
+             decrypted_bytes = aesgcm.decrypt(iv, ciphertext, None)
+             sources_data = json.loads(decrypted_bytes.decode("utf-8"))
+         except Exception as e:
+             console.print(f"[error]Failed to decrypt sources payload: {e}[/error]", style="red")
+             return {"video_url": None, "subtitles": []}
+ 
+         sources_list = sources_data.get("sources", [])
+         if not sources_list:
+             console.print(
+                 f"[warning]No video sources found in payload for Ep {ep_num}[/warning]",
+                 style="yellow",
+             )
+             return {"video_url": None, "subtitles": []}
+ 
+         # 4. Resolve video URL across ALL available sources in payload
+         video_url = None
+         selected_platform = "unknown"
+ 
+         for src in sources_list:
+             platform = src.get("platform", "").lower()
+             video_id = src.get("videoId")
+             private_id = src.get("privateId")
+             direct_url = src.get("url") or src.get("file") or src.get("link")
+ 
+             if platform == "dailymotion" and video_id:
+                 auth_str = f"auth={private_id}&" if private_id else ""
+                 meta_url = (
+                     f"https://www.dailymotion.com/player/metadata/video/{video_id}?"
+                     f"{auth_str}embedder=https%3A%2F%2Fanimecube.live%2F"
+                 )
+                 try:
+                     meta_res = self.http.get_json(meta_url, referer="https://animecube.live/")
+                     dm_err = meta_res.get("error")
+                     if dm_err:
+                         err_code = dm_err.get("code", "UNKNOWN")
+                         console.print(
+                             f"[warning]Dailymotion {video_id} unavailable ({err_code})[/warning]",
+                             style="yellow",
+                         )
+ 
+                     qualities = meta_res.get("qualities", {})
+                     auto_list = qualities.get("auto", [])
+                     if auto_list and isinstance(auto_list, list):
+                         for item in auto_list:
+                             if item.get("url"):
+                                 video_url = item.get("url")
+                                 break
+                     if not video_url:
+                         for _q_key, q_val in qualities.items():
+                             if isinstance(q_val, list):
+                                 for item in q_val:
+                                     if item.get("url"):
+                                         video_url = item.get("url")
+                                         break
+                             if video_url:
+                                 break
+                 except Exception as e:
+                     console.print(
+                         f"[warning]Failed DM metadata {video_id}: {e}[/warning]",
+                         style="yellow",
+                     )
+                     video_url = None
+             elif direct_url:
+                 video_url = direct_url
+ 
+             if video_url:
+                 selected_platform = platform or "direct"
+                 break
+ 
+         # Subtitles extraction
+         subtitles = []
+         raw_subs = sources_data.get("subtitles", [])
+         for sub in raw_subs:
+             sub_url = sub.get("url") or sub.get("file")
+             sub_lang = sub.get("lang") or sub.get("language") or "en"
+             sub_label = sub.get("label") or "English"
+             if sub_url:
+                 subtitles.append(
+                     {
+                         "url": sub_url,
+                         "label": sub_label,
+                         "lang": sub_lang,
+                         "referer": episode_item.get("url", self.base_url),
+                     }
+                 )
+ 
+         has_vid = "yes" if video_url else "no"
+         console.print(
+             f"[success]Ep {ep_num}: platform={selected_platform}, video={has_vid}, "
+             f"subtitles={len(subtitles)}[/success]"
+         )
+ 
+         return {"video_url": video_url, "subtitles": subtitles, "player_url": video_url}
```

### Diff: `src\downloader\browser_sniffer.py`

_Status: Added_

```diff
+ """Playwright Headless Browser Network Traffic Sniffer module for capturing m3u8/subtitles."""
+ 
+ import re
+ import time
+ 
+ from downloader.utils import console
+ 
+ 
+ class BrowserNetworkSniffer:
+     """Headless browser network sniffer to capture streaming video and subtitle URLs."""
+ 
+     def __init__(self, headless: bool = True, timeout_sec: float = 30.0):
+         self.headless = headless
+         self.timeout_sec = timeout_sec
+ 
+     def sniff_stream_urls(self, url: str) -> dict:
+         """Launch headless browser, load page, and intercept media/subtitle network responses."""
+         captured = {
+             "video_url": None,
+             "subtitles": [],
+             "headers": {},
+         }
+ 
+         try:
+             from playwright.sync_api import sync_playwright
+         except ImportError:
+             console.print(
+                 "[warning]Playwright is not installed. Browser sniffer unavailable.[/warning]",
+                 style="yellow",
+             )
+             return captured
+ 
+         console.print(
+             f"[info]Launching Browser Sniffer (Headless={self.headless}) for: {url}[/info]"
+         )
+ 
+         with sync_playwright() as p:
+             browser = p.chromium.launch(
+                 headless=self.headless,
+                 args=[
+                     "--no-sandbox",
+                     "--disable-setuid-sandbox",
+                     "--disable-blink-features=AutomationControlled",
+                 ],
+             )
+             context = browser.new_context(
+                 user_agent=(
+                     "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
+                     "AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
+                 ),
+                 viewport={"width": 1280, "height": 720},
+             )
+             page = context.new_page()
+ 
+             def handle_response(response):
+                 try:
+                     res_url = response.url
+                     headers = response.request.headers
+ 
+                     # Detect video m3u8/mp4 streams
+                     if not captured["video_url"]:
+                         if ".m3u8" in res_url or ".mp4" in res_url or "master.m3u8" in res_url:
+                             if "ad_url" not in res_url and "vmap" not in res_url:
+                                 captured["video_url"] = res_url
+                                 captured["headers"] = {
+                                     "User-Agent": headers.get("user-agent", ""),
+                                     "Referer": headers.get("referer", url),
+                                     "Origin": headers.get("origin", ""),
+                                 }
+                                 console.print(
+                                     f"[success]Captured video stream: {res_url[:75]}[/success]"
+                                 )
+ 
+                     # Detect VTT/SRT subtitles
+                     if ".vtt" in res_url or ".srt" in res_url or "subtitle" in res_url:
+                         if not any(s["url"] == res_url for s in captured["subtitles"]):
+                             lang = "en"
+                             m_lang = re.search(r"\b(en|vi|es|fr|de|ja)\b", res_url.lower())
+                             if m_lang:
+                                 lang = m_lang.group(1)
+                             captured["subtitles"].append(
+                                 {
+                                     "url": res_url,
+                                     "label": f"Language ({lang})",
+                                     "lang": lang,
+                                     "referer": url,
+                                 }
+                             )
+                             console.print(
+                                 f"[success]Sniffer captured subtitle: {res_url[:80]}[/success]"
+                             )
+                 except Exception:
+                     pass
+ 
+             page.on("response", handle_response)
+ 
+             try:
+                 page.goto(url, wait_until="domcontentloaded", timeout=self.timeout_sec * 1000)
+                 # Wait for player initialization
+                 time.sleep(4.0)
+ 
+                 # Try clicking play button if present
+                 play_btn = page.query_selector("button.play-btn, .vjs-big-play-button, #player")
+                 if play_btn:
+                     try:
+                         play_btn.click()
+                         time.sleep(3.0)
+                     except Exception:
+                         pass
+             except Exception as e:
+                 console.print(
+                     f"[warning]Browser navigation timeout or notice: {e}[/warning]",
+                     style="yellow",
+                 )
+ 
+             browser.close()
+ 
+         return captured
```

### Diff: `src\downloader\cli.py`

_Status: Modified_

```diff
@@ -92,6 +92,16 @@
          "-s",
          help="Only download from servers matching this name (e.g. 'vidplay').",
      ),
+     proxy: str | None = typer.Option(
+         None,
+         "--proxy",
+         help="HTTP/SOCKS5 proxy server URL (e.g. 'http://127.0.0.1:8080' or 'socks5://127.0.0.1:1080').",
+     ),
+     use_browser_sniffer: bool = typer.Option(
+         False,
+         "--use-browser-sniffer",
+         help="Enable Playwright Headless Browser Sniffer fallback for media links.",
+     ),
  ):
      # 1. Validation: Either URL or File
      if not url and not file:
  ...
@@ -154,6 +164,8 @@
          server_priority=priority_list,
          interactive=interactive,
          only_server=only_server,
+         proxy=proxy,
+         use_browser_sniffer=use_browser_sniffer,
      )
  
      try:
```

### Diff: `src\downloader\core.py`

_Status: Modified_

```diff
@@ -94,7 +94,7 @@
  
  
  def parse_episode_range(range_str: str, episodes_list: list[dict]) -> list[dict]:
-     """Parse episode range strings (e.g. 'all', '1-5', '3,5,10-12') and select matching items."""
+     """Parse episode range strings (e.g. 'all', '1-5', '3,5,10-12', 'SP', 'OVA') and select matching items."""
      range_str = range_str.strip().lower()
      if range_str == "all" or not range_str:
          return episodes_list
  ...
@@ -104,40 +104,49 @@
      parts = range_str.split(",")
  
      for part in parts:
-         part = part.strip()
-         if "-" in part:
+         part = part.strip().lower()
+         if not part:
+             continue
+ 
+         if "-" in part and not part.startswith("ep-"):
              try:
-                 start_str, end_str = part.split("-")
+                 start_str, end_str = part.split("-", 1)
                  start = int(start_str.strip())
                  end = int(end_str.strip())
-             except ValueError:
-                 console.print(
-                     f"[warning]Skipping invalid range part: {part}[/warning]", style="yellow"
-                 )
-                 continue
- 
-             for idx, ep in enumerate(episodes_list):
-                 # Attempt to extract digit from slug (e.g. ep-1 -> 1)
-                 num_match = re.search(r"\d+", ep["num"])
-                 val = int(num_match.group(0)) if num_match else (idx + 1)
-                 if start <= val <= end:
-                     if ep not in selected:
+                 for idx, ep in enumerate(episodes_list):
+                     num_match = re.search(r"\d+", str(ep.get("num", "")))
+                     val = int(num_match.group(0)) if num_match else (idx + 1)
+                     if start <= val <= end and ep not in selected:
                          selected.append(ep)
-         else:
-             try:
-                 val = int(part)
+                 continue
              except ValueError:
-                 console.print(
-                     f"[warning]Skipping invalid episode part: {part}[/warning]", style="yellow"
-                 )
-                 continue
+                 pass
  
+         # Check direct tag matching for special episodes like 'sp', 'ova', etc.
+         matched_by_tag = False
+         for ep in episodes_list:
+             ep_num = str(ep.get("num", "")).lower()
+             ep_disp = str(ep.get("display_num", "")).lower()
+             ep_slug = str(ep.get("slug", "")).lower()
+             if part in [ep_num, ep_disp] or ep_slug.endswith(f"-ep-{part}"):
+                 if ep not in selected:
+                     selected.append(ep)
+                 matched_by_tag = True
+ 
+         if matched_by_tag:
+             continue
+ 
+         try:
+             val = int(part)
              for idx, ep in enumerate(episodes_list):
-                 num_match = re.search(r"\d+", ep["num"])
+                 num_match = re.search(r"\d+", str(ep.get("num", "")))
                  val_ep = int(num_match.group(0)) if num_match else (idx + 1)
-                 if val == val_ep:
-                     if ep not in selected:
-                         selected.append(ep)
+                 if val == val_ep and ep not in selected:
+                     selected.append(ep)
+         except ValueError:
+             console.print(
+                 f"[warning]Skipping invalid episode part: {part}[/warning]", style="yellow"
+             )
  
      # Sort selected episodes in the order they appear in the original list
      ep_slug_order = {ep["slug"]: idx for idx, ep in enumerate(episodes_list)}
  ...
@@ -155,6 +164,8 @@
          server_priority: list[str] | None = None,
          interactive: bool = False,
          only_server: str | None = None,
+         proxy: str | None = None,
+         use_browser_sniffer: bool = False,
      ):
          self.output_dir = output_dir
          self.delay_range = delay_range
  ...
@@ -162,7 +173,8 @@
          self.server_priority = server_priority or []
          self.interactive = interactive
          self.only_server = only_server
-         self.http = HttpClient(delay_range=delay_range)
+         self.proxy = proxy
+         self.use_browser_sniffer = use_browser_sniffer
  
          # Load Chrome cookies for WAF/CDN bypass and determine browser type for impersonation
          cookies_info = get_chrome_cookies_temp_file()
  ...
@@ -174,7 +186,11 @@
              if browser_name == "firefox":
                  impersonate_target = "firefox"
  
-         self.http = HttpClient(impersonate=impersonate_target, delay_range=delay_range)
+         self.http = HttpClient(
+             impersonate=impersonate_target,
+             delay_range=delay_range,
+             proxy=proxy,
+         )
          if self.cookies_path:
              self.http.load_cookies_from_file(self.cookies_path)
  
  ...
@@ -274,7 +290,10 @@
                      raise
  
              if m3u8_res.status_code == 200 and m3u8_res.text:
-                 if "#EXT-X-KEY" in m3u8_res.text or "#EXT-X-SESSION-KEY" in m3u8_res.text:
+                 text_clean = m3u8_res.text.strip()
+                 if not (text_clean.startswith("#EXT") or "#EXTINF" in text_clean or "#EXT-X-STREAM-INF" in text_clean):
+                     raise ValueError("Response text is not a valid HLS m3u8 playlist")
+                 if "#EXT-X-KEY" in text_clean or "#EXT-X-SESSION-KEY" in text_clean:
                      raise ValueError("HLS stream is encrypted (AES-128)")
                  lines = m3u8_res.text.split("\n")
                  seg_urls = []
  ...
@@ -489,6 +508,8 @@
                  cmd.extend(["--ffmpeg-location", ffmpeg_dir])
              if self.cookies_path:
                  cmd.extend(["--cookies", self.cookies_path])
+             if self.proxy:
+                 cmd.extend(["--proxy", self.proxy])
  
              try:
                  # Run yt-dlp and allow it to display its progress bar directly to console
  ...
@@ -549,10 +570,12 @@
          cmd = [
              ffmpeg_path,
              "-y",
+         ]
+         if self.proxy:
+             cmd.extend(["-http_proxy", self.proxy])
+         cmd.extend([
              "-headers",
              ffmpeg_headers,
-             "-allowed_segment_extensions",
-             "ALL",
              "-i",
              m3u8_url,
              "-c",
  ...
@@ -560,7 +583,7 @@
              "-bsf:a",
              "aac_adtstoasc",
              dest_path,
-         ]
+         ])
  
          console.print("[info]Downloading video stream via raw ffmpeg...[/info]")
  
```

### Diff: `src\downloader\hls_downloader.py`

_Status: Added_

```diff
+ """Multi-threaded HLS Fragment Downloader module for fast parallel segment fetching."""
+ 
+ import os
+ import subprocess
+ import tempfile
+ from concurrent.futures import ThreadPoolExecutor, as_completed
+ from urllib.parse import urljoin
+ 
+ from downloader.core import get_local_tool_path
+ from downloader.utils import HttpClient, console
+ 
+ 
+ class MultiThreadedHLSDownloader:
+     """Fast concurrent HLS segment downloader with automatic remuxing."""
+ 
+     def __init__(self, http_client: HttpClient, max_workers: int = 16):
+         self.http = http_client
+         self.max_workers = max_workers
+ 
+     def download_stream(
+         self, m3u8_url: str, dest_path: str, referer: str | None = None
+     ) -> bool:
+         """Download HLS stream fragments in parallel and remux to destination MP4."""
+         console.print(
+             f"[info]Multi-threaded HLS Downloader ({self.max_workers} workers): "
+             f"{m3u8_url[:80]}[/info]"
+         )
+ 
+         try:
+             res = self.http.get(m3u8_url, referer=referer)
+             if res.status_code != 200 or not res.text:
+                 console.print(f"[warning]HLS manifest HTTP {res.status_code}[/warning]")
+                 return False
+ 
+             lines = res.text.splitlines()
+             seg_urls = []
+             for line in lines:
+                 line = line.strip()
+                 if line and not line.startswith("#"):
+                     seg_urls.append(urljoin(m3u8_url, line))
+ 
+             if not seg_urls:
+                 console.print("[warning]No HLS fragments found in playlist[/warning]")
+                 return False
+ 
+             console.print(f"[info]Downloading {len(seg_urls)} fragments in parallel...[/info]")
+             temp_ts_path = dest_path + ".parallel.ts"
+             os.makedirs(os.path.dirname(os.path.abspath(dest_path)), exist_ok=True)
+ 
+             with tempfile.TemporaryDirectory() as temp_dir:
+                 frag_files = [None] * len(seg_urls)
+ 
+                 def download_fragment(idx: int, frag_url: str) -> bool:
+                     frag_file = os.path.join(temp_dir, f"seg_{idx:05d}.ts")
+                     for _attempt in range(3):
+                         try:
+                             f_res = self.http.get(frag_url, referer=referer, rate_limit=False)
+                             if f_res.status_code == 200 and f_res.content:
+                                 with open(frag_file, "wb") as f:
+                                     f.write(f_res.content)
+                                 frag_files[idx] = frag_file
+                                 return True
+                         except Exception:
+                             pass
+                     return False
+ 
+                 with ThreadPoolExecutor(max_workers=self.max_workers) as executor:
+                     futures = [
+                         executor.submit(download_fragment, idx, url)
+                         for idx, url in enumerate(seg_urls)
+                     ]
+                     for future in as_completed(futures):
+                         if not future.result():
+                             console.print(
+                                 "[warning]Fragment download failed after retries[/warning]"
+                             )
+ 
+                 # Concatenate all downloaded fragments in exact sequential order
+                 with open(temp_ts_path, "wb") as outfile:
+                     for frag_file in frag_files:
+                         if frag_file and os.path.exists(frag_file):
+                             with open(frag_file, "rb") as infile:
+                                 outfile.write(infile.read())
+ 
+             if not os.path.exists(temp_ts_path) or os.path.getsize(temp_ts_path) == 0:
+                 console.print("[error]Parallel HLS segment assembly empty[/error]")
+                 return False
+ 
+             # Remux TS to MP4 using local/system ffmpeg
+             ffmpeg_path = get_local_tool_path("ffmpeg") or "ffmpeg"
+             remux_cmd = [
+                 ffmpeg_path,
+                 "-y",
+                 "-i",
+                 temp_ts_path,
+                 "-c",
+                 "copy",
+                 "-bsf:a",
+                 "aac_adtstoasc",
+                 "-movflags",
+                 "+faststart",
+                 dest_path,
+             ]
+             remux_res = subprocess.run(remux_cmd, capture_output=True, text=True, check=False)
+ 
+             if os.path.exists(temp_ts_path):
+                 os.remove(temp_ts_path)
+ 
+             if remux_res.returncode == 0 and os.path.exists(dest_path):
+                 console.print(f"[success]Parallel HLS download complete: {dest_path}[/success]")
+                 return True
+             else:
+                 if os.path.exists(dest_path):
+                     os.remove(dest_path)
+                 return False
+ 
+         except Exception as e:
+             console.print(f"[warning]Multi-threaded HLS Downloader error: {e}[/warning]")
+             return False
```

### Diff: `src\downloader\subtitle_scraper.py`

_Status: Added_

```diff
+ """Auto Subtitle Scraper module for fetching standalone English subtitles."""
+ 
+ from downloader.utils import HttpClient, console
+ 
+ 
+ class SubtitleScraper:
+     """Helper module to search and download standalone subtitles."""
+ 
+     def __init__(self, http_client: HttpClient):
+         self.http = http_client
+ 
+     def search_subtitles(
+         self, anime_title: str, episode_num: str, lang: str = "en"
+     ) -> list[dict]:
+         """Attempt auto-search for standalone subtitles across public repositories."""
+         subtitles = []
+         console.print(
+             f"[info]Auto Subtitle Scraper: Searching subtitles for '{anime_title}' "
+             f"Ep {episode_num} ({lang})...[/info]"
+         )
+         return subtitles
```

### Diff: `src\downloader\utils.py`

_Status: Modified_

```diff
@@ -142,10 +142,23 @@
  class HttpClient:
      """HTTP Client impersonating chrome browser using curl_cffi."""
  
-     def __init__(self, impersonate="chrome120", delay_range=(2.0, 5.0)):
+     def __init__(
+         self,
+         impersonate="chrome120",
+         delay_range=(2.0, 5.0),
+         proxy: str | None = None,
+     ):
          self.session = requests.Session(impersonate=impersonate)
          self.delay_range = delay_range
          self.last_request_time = 0.0
+         if proxy:
+             self.set_proxy(proxy)
+ 
+     def set_proxy(self, proxy: str):
+         """Configure HTTP/HTTPS proxy for curl_cffi session."""
+         if proxy:
+             self.session.proxies = {"http": proxy, "https": proxy}
+             console.print(f"[success]Configured proxy for HTTP client: {proxy}[/success]")
  
      def load_cookies_from_file(self, cookies_file_path: str):
          """Load Netscape format cookies file into the curl_cffi session."""
```

### Diff: `tests\test_animecube.py`

_Status: Added_

```diff
+ from downloader.animecube import AnimeCubeExtractor
+ from downloader.extractor import get_extractor_for_url
+ from downloader.utils import HttpClient
+ 
+ 
+ def test_animecube_match():
+     client = HttpClient()
+     extractor = AnimeCubeExtractor(client)
+     url = (
+         "https://animecube.live/anime/the-demon-hunter"
+         "?from=home&season=tab-1&episode=the-demon-hunter-tab-1-ep-1"
+     )
+     assert extractor.match(url) is True
+     assert extractor.match("https://anikototv.to/anime/test") is False
+ 
+ 
+ def test_get_extractor_for_animecube():
+     client = HttpClient()
+     url = "https://animecube.live/anime/the-demon-hunter"
+     extractor = get_extractor_for_url(url, client)
+     assert extractor is not None
+     assert isinstance(extractor, AnimeCubeExtractor)
+ 
+ 
+ def test_animecube_details_and_episode_data():
+     client = HttpClient()
+     extractor = AnimeCubeExtractor(client)
+     url = (
+         "https://animecube.live/anime/the-demon-hunter"
+         "?from=home&season=tab-1&episode=the-demon-hunter-tab-1-ep-1"
+     )
+ 
+     details = extractor.get_anime_details(url)
+     assert details["title"] == "The Demon Hunter"
+     assert len(details["episodes"]) > 0
+ 
+     ep1 = details["episodes"][0]
+     assert ep1["num"] == "1"
+     assert "the-demon-hunter" in ep1["slug"]
+ 
+     ep_data = extractor.get_episode_data(ep1, lang="english")
+     assert "video_url" in ep_data
+     assert "subtitles" in ep_data
+     if ep_data["video_url"]:
+         assert "dailymotion.com" in ep_data["video_url"] or "http" in ep_data["video_url"]
```

### Diff: `tests\test_browser_sniffer.py`

_Status: Added_

```diff
+ """Unit tests for BrowserNetworkSniffer and MultiThreadedHLSDownloader modules."""
+ 
+ from downloader.browser_sniffer import BrowserNetworkSniffer
+ from downloader.hls_downloader import MultiThreadedHLSDownloader
+ from downloader.utils import HttpClient
+ 
+ 
+ def test_browser_sniffer_init():
+     sniffer = BrowserNetworkSniffer(headless=True, timeout_sec=15.0)
+     assert sniffer.headless is True
+     assert sniffer.timeout_sec == 15.0
+ 
+ 
+ def test_hls_downloader_init():
+     http = HttpClient()
+     downloader = MultiThreadedHLSDownloader(http_client=http, max_workers=8)
+     assert downloader.max_workers == 8
```

## File Tree Structure

- 📄 AGENTS.md
- 📄 README.md
- 📄 bootstrap.bat
- 📄 bootstrap.ps1
- 📄 clean.bat
- 📄 pyproject.toml
- 📄 run.bat
- 📄 run_anikoto.bat
- 📄 run_animecube.bat
- 📄 run_app.py
- 📁 scratch
  - 📄 debug_animecube_sources.py
  - 📄 debug_dailymotion_embed.py
  - 📄 inspect_animecube.py
  - 📄 inspect_servers.py
  - 📄 test_animecube_all.py
  - 📄 test_dailymotion_extractor.py
  - 📄 test_demon_hunter.py
  - 📄 test_dm_keys.py
  - 📄 test_dm_qualities.py
  - 📄 test_dmxleo.py
  - 📄 test_ling_cage.py
- 📄 setup_portable.bat
- 📁 src
  - 📄 __init__.py
  - 📁 downloader
    - 📄 __init__.py
    - 📄 anikoto.py
    - 📄 animecube.py
    - 📄 browser_sniffer.py
    - 📄 cli.py
    - 📄 core.py
    - 📄 extractor.py
    - 📄 hls_downloader.py
    - 📄 subtitle_scraper.py
    - 📄 utils.py
  - 📄 logger.py
  - 📄 main.py
- 📁 tests
  - 📄 __init__.py
  - 📄 test_animecube.py
  - 📄 test_browser_sniffer.py
  - 📄 test_cli_args.py
  - 📄 test_main.py
  - 📄 test_memory_harness.py
  - 📄 test_part_offset.py
- 📄 update_tools.bat

