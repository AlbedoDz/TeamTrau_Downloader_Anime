[project]
name = "joyful-babbage"
version = "0.1.0"
description = "High-performance Python 3.11+ template project for Antigravity 2.0+ on Windows"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.4.0",
    "python-dotenv>=1.0.1",
    "beautifulsoup4>=4.12.0",
    "curl-cffi>=0.6.0",
    "cryptography>=42.0.0",
    "rich>=13.0.0",
    "typer>=0.9.0",
    "pywebview>=6.2.1",
    "pyinstaller>=6.22.2",
]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --tb=short"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
    ".git",
    ".venv",
    ".pytest_cache",
    ".ruff_cache",
    ".agent",
]


[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "RUF", # Ruff-specific rules
]
ignore = ["E501", "N801"]

[tool.ruff.lint.isort]
combine-as-imports = true
section-order = ["future", "standard-library", "third-party", "first-party", "local-folder"]
